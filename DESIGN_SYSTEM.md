# DESIGN_SYSTEM.md — BMS

## 🔴 ประกาศจากเจ้าของโปรเจค (2026-09-02)

> **"การตกแต่งรูปแบบในหน้าล็อกอิน (`/login`) คือรูปแบบที่ใช้ทั้งระบบ**
> **ทุกหน้าที่ทำต่อจากนี้ต้องใช้รูปแบบเดียวกันนี้ทั้งหมด จะได้ไม่ต้องเขียนหน้า Frontend หลายรอบ"**

**หน้า `/login` คือหน้าอ้างอิงมาตรฐาน (reference implementation)** — ถ้าไม่แน่ใจว่าปุ่ม ช่องกรอก
ความโค้ง สี หรือระยะห่างควรเป็นแบบไหน ให้เปิดหน้านั้นดูแล้วทำตาม

### แปลว่าอะไรในทางปฏิบัติ

| สิ่งที่ต้องทำ | สิ่งที่ห้ามทำ |
|---|---|
| เรียกใช้ token และ class ที่มีอยู่แล้วใน [`theme.blade.php`](resources/views/layouts/theme.blade.php) | ห้ามคิดสไตล์ใหม่เองต่อหน้า · ห้ามก็อป CSS จากที่อื่นมาแปะ |
| หน้าใหม่ `@extends('layouts.app')` ให้ได้แถบบน/เมนู/แถบสถานะฟรี | ห้ามเขียนโครง `<html>` หรือแถบบนของตัวเอง |
| อยากได้ค่าใหม่ → เพิ่ม token ที่ธีมกลาง แล้วทุกหน้าได้ตาม | ห้ามเขียนสี/ขนาด/ความโค้งเป็นค่าดิบในหน้า |
| อยากปรับหน้าตาทั้งระบบ → แก้ที่ `theme.blade.php` ที่เดียว | ห้ามไล่แก้ทีละหน้า |

**ผลที่ได้:** เจ้าของบอกครั้งเดียวว่าอยากได้แบบไหน (เช่น "ลดความโค้ง") แล้วแก้ที่ token จุดเดียว
**ทุกหน้าในระบบเปลี่ยนตามพร้อมกันทันที** ไม่ต้องมานั่งแต่ง Frontend ซ้ำทีละหน้า

### ที่มาของรูปแบบ

- ภาพต้นแบบที่เจ้าของอนุมัติ: `ให้วิเคราะห์/Frontend/หน้าล็อคอิน/` และ `ให้วิเคราะห์/Frontend/หน้าเมนู/`
- หน้าที่ทำตามต้นแบบแล้ว (ใช้เป็นตัวอย่างได้): `/login` · `/dashboard` · `/admin/external-accounts`
- ไฟล์ที่บังคับใช้จริง: [`resources/views/layouts/theme.blade.php`](resources/views/layouts/theme.blade.php)

---

## 1. กฎเหล็ก 5 ข้อ

1. **ห้ามเขียนค่าสี / ขนาด / เงา / รัศมี เป็นค่าดิบในหน้าใดๆ**
   ต้องเรียกผ่านตัวแปรใน `theme.blade.php` เท่านั้น
   ต้องการค่าใหม่ → **เพิ่ม token ที่ธีมกลาง** ไม่ใช่เขียนดิบในหน้า
   ```css
   /* ❌ ผิด */   color: #12306b;  border-radius: 10px;
   /* ✅ ถูก */   color: var(--navy-800);  border-radius: var(--radius);
   ```
   *ข้อยกเว้นเดียว:* สีเฉพาะไอคอนแต่ละตัวในแถบเครื่องมือ (เขียว/ฟ้า/ทอง) — ประกาศไว้ในอาเรย์ `$tools` ที่เดียว

2. **ทุกหน้าเนื้อหาต้อง `@extends('layouts.app')`** — ห้ามเขียนโครง `<html>` เอง
   ยกเว้นหน้าที่ไม่มีเมนู (ล็อกอิน) ให้เขียนโครงเอง แต่ยังต้อง `@include('layouts.theme')` และ `@include('layouts.i18n')`

3. **ทุกข้อความที่ผู้ใช้เห็นต้องมีครบ 2 ภาษา ไทย + อังกฤษ** (ดูข้อ 6)

4. **ตัวสลับภาษาต้องมีทุกหน้า** — `@include('layouts.partials.lang-switch')`

5. **ตารางที่กว้างต้องเลื่อนในกรอบตัวเอง** (`.table-scroll`) — หน้าเว็บห้ามเลื่อนแนวนอน

---

## 2. โทนและตัวตนของระบบ

BMS เป็นสไตล์ **ERP desktop** — ต่างจาก Insight และ QuoteCompare โดยตั้งใจ

| ควรเป็น | ไม่ควรเป็น |
|---|---|
| แน่น ข้อมูลเยอะ อ่านเร็ว เหมือนโปรแกรมบนเดสก์ท็อป | โปร่ง ช่องไฟเยอะแบบเว็บการตลาด |
| น้ำเงินองค์กรเป็นสีหลัก ใช้สีอื่นเฉพาะบอกสถานะ | ไล่สีฉูดฉาด · gradient hero · emoji เป็นไอคอน |
| ไอคอนเส้น (stroke) ขนาด 16–19px | ไอคอนตันหลากสี · ไอคอนขนาดไม่เท่ากันในแถวเดียว |
| มุมมนน้อย (6–10px) เงาบาง | มุมมนเยอะ · เงาฟุ้ง · การ์ดซ้อนการ์ด |

### กฎการจัดหน้า (เจ้าของสั่ง 2026-09-02)

- **เรื่องเดียว = การ์ดเดียว** อย่าซอยเป็นหลายการ์ดถ้ามันคือเรื่องเดียวกัน
  ใช้เส้นคั่นแบ่ง section ภายในการ์ดแทน (ดูตัวอย่างที่ [`core/profile.blade.php`](resources/views/core/profile.blade.php))
- **ห้ามแสดงข้อมูลซ้ำในหน้าเดียวกัน** อะไรที่โชว์ในหัวการ์ดแล้ว ห้ามเอาลงตารางข้างล่างอีก
- **ป้ายสถานะ (`.pill`) ใส่เฉพาะตอนที่มันบอกอะไรจริงๆ** เช่นสถานะเอกสาร สถานะการเชื่อมต่อ
  ป้ายที่บอกสิ่งที่เห็นอยู่แล้ว (`อ่านอย่างเดียว` · `ผู้ใช้งานทั่วไป` · `ทำงานอยู่`) **ไม่ต้องใส่** — รกเปล่าๆ
- **ข้อความ 2 ภาษาให้สลับด้วยตัวเปลี่ยนภาษา ไม่ใช่แสดงพร้อมกันทั้ง 2 บรรทัด**
  ใช้ `data-loc-th` / `data-loc-en` แถวเดียวพอ อย่าทำแถว `ชื่อ (ไทย)` + `ชื่อ (อังกฤษ)` คู่กัน
- **คำอธิบายให้สั้นที่สุดเท่าที่ยังเข้าใจ** ไม่ต้องเขียนย่อหน้ายาว

### 🔴 เนื้อหาทุกหน้าต้องอยู่กึ่งกลางและสมมาตร (เจ้าของสั่ง 2026-09-02)

> _"มันชิดกับเมนูทางซ้ายเกินไป จัดให้พอดีสมมาตรสิ และพอเลื่อนปิดเมนู ค่อยเลื่อนมาตำแหน่งกลาง —_
> _จุดนี้จะต้องทำทุกหน้าแนวนี้"_

ทำไว้ให้แล้วใน `layouts/app.blade.php` — **หน้าใหม่ไม่ต้องทำอะไรเพิ่ม** แค่ `@extends('layouts.app')`

```
<main class="content">        ← พื้นที่ที่เหลือจากเมนู · เว้นขอบ 20px 26px
  <div class="page">          ← max-width: var(--page-max) · margin-inline: auto
    @yield('content')
  </div>
</main>
```

- ค่าเริ่มต้น `--page-max: 1180px` (ตั้งใน `theme.blade.php`)
- หน้าไหนอยากแคบกว่านั้น ให้ override **ที่ `:root`** ใน `@push('page-style')` ของหน้านั้น
  ```css
  :root { --page-max: 900px; }   /* ตัวอย่างจากหน้า /profile */
  ```
  ⚠️ ต้องประกาศที่ `:root` เท่านั้น — ตัวแปร CSS ไหลลงล่างอย่างเดียว
  ถ้าประกาศไว้ในลูก (`.profile { --page-max: … }`) ตัวแม่ `.page` จะไม่เห็นค่า

**พอย่อ/ปิดเมนูซ้าย เนื้อหาเลื่อนไปอยู่กลางพื้นที่ใหม่เองอัตโนมัติ** ไม่ต้องเขียน JS
(วัดจริงที่จอ 1440px: เมนูกาง → ช่องว่างซ้าย/ขวา 154/154px · เมนูย่อ → 244/244px · การ์ดเลื่อนซ้าย 90px)

**อย่าใส่ `max-width` เองที่การ์ดหรือ wrapper ของหน้า** ให้ใช้ `--page-max` ทางเดียว
ไม่งั้นเวลาแก้ความกว้างทั้งระบบจะต้องไล่แก้ทีละหน้า

---

## 3. Token ทั้งหมด (จาก `theme.blade.php`)

### สี

| Token | ค่า | ใช้ตอนไหน |
|---|---|---|
| `--navy-900` | `#0d2350` | แถบบน (ฝั่งซ้าย) · ปุ่มหลักตอน hover |
| `--navy-800` | `#12306b` | **ปุ่มหลัก · หัวข้อการ์ด · ตัวเลขเน้น** |
| `--navy-700` | `#1c4189` | แถบบน (ฝั่งขวา ไล่สี) |
| `--royal` / `--royal-400` | `#2f57a6` / `#4a6bb5` | แผงแบรนด์หน้าล็อกอิน · แถบ hero |
| `--accent` | `#2563c7` | ลิงก์ · เส้นขอบตอน focus · จุดเน้น |
| `--accent-soft` | `#e8f0fc` | พื้นของรายการเมนูที่กำลังเปิดอยู่ |
| `--page` | `#eef1f7` | พื้นหลังนอกการ์ด |
| `--surface` | `#ffffff` | การ์ด · แผงเนื้อหา · เมนูซ้าย |
| `--surface-2` | `#f7f9fc` | แถบเครื่องมือ · หัวตาราง · hover |
| `--surface-3` | `#eef2f8` | แถบสถานะล่าง |
| `--ink` / `--ink-soft` / `--muted` | `#16233d` / `#45556f` / `#6b7a94` | ข้อความหลัก / รอง / ป้ายกำกับ |
| `--line` / `--line-soft` | `#dde4ef` / `#eaeff6` | เส้นขอบ / เส้นคั่นในตาราง |
| `--line-dark` | `#97a6bf` | เส้นที่ต้องเห็นชัด — กิ่งผังเมนูซ้าย · ตัวคั่น breadcrumb |
| `--ok` `--warn` `--danger` `--info` | | สีบอกสถานะ (แต่ละตัวมี `-soft` คู่กันสำหรับพื้นป้าย) |
| `--folder` / `--folder-dark` | `#efb245` / `#d99a24` | ไอคอนโฟลเดอร์ในเมนู (โทนทองแบบ ERP) |

> ทุกคู่สีตัวอักษร/พื้นในตารางนี้ผ่าน **WCAG AA** แล้ว — เปลี่ยนค่าเมื่อไหร่ต้องเช็ค contrast ใหม่

### ตัวอักษร

```
--font: "Segoe UI", "Noto Sans Thai", "Leelawadee UI", system-ui, sans-serif
--fs-xs 11.5px   ป้ายสถานะ · แถบสถานะล่าง
--fs-sm 12.5px   ตาราง · เมนู · คำอธิบาย
--fs-base 14px   ข้อความทั่วไป · ปุ่ม   ← ค่าเริ่มต้นของ body
--fs-md 15px     หัวข้อการ์ด
--fs-lg 18px     หัวข้อส่วน
--fs-xl 24px     หัวข้อหน้า
```
ตัวหนาใช้แค่ **600** (เน้นปกติ) และ **700–800** (หัวข้อ/ตัวเลข) — ไม่ใช้ค่าอื่น

### รูปทรง · เงา · ขนาดโครงหน้า

> 🔴 **เจ้าของไม่ชอบมุมโค้งมาก (สั่ง 2026-09-02)** — มุมทั้งระบบคุมจาก 3 ค่านี้ที่เดียว
> อยากปรับความโค้งทั้งระบบให้แก้ที่นี่ **ห้ามใส่ `border-radius` เป็นตัวเลขดิบในหน้า**

```
--radius-sm 3px    ปุ่มไอคอน · ป้าย · รายการเมนู · ธงภาษา
--radius    4px    ปุ่ม · ช่องกรอก · การ์ด
--radius-lg 8px    การ์ดใหญ่กลางจอ

--shadow-sm  เงาการ์ดปกติ
--shadow     เมนูลอย (dropdown)
--shadow-lg  ลิ้นชักบนมือถือ

--topbar-h 58px   --toolbar-h 44px   --statusbar-h 30px
--sidebar-w 232px --sidebar-w-collapsed 52px
```

### ลำดับชั้นการซ้อน — ห้ามใส่ตัวเลข z-index มั่วนอกสเกลนี้

```
--z-sticky 20 · --z-scrim 30 · --z-drawer 40 · --z-dropdown 50 · --z-modal 60
```

---

## 4. ชิ้นส่วนที่มีให้ใช้แล้ว (อย่าสร้างซ้ำ)

| Class | คืออะไร |
|---|---|
| `.btn` | ปุ่มหลัก (น้ำเงินเข้ม) · `.btn-block` เต็มความกว้าง |
| `.btn-outline` | ปุ่มรอง มีกรอบน้ำเงิน |
| `.btn-quiet` | ปุ่มเงียบ (ยกเลิก · ปิด) |
| `.field` + `.input` | ช่องกรอก · `.input-wrap > svg.lead` ไอคอนซ้าย · `.trail` ปุ่มขวา (เช่นปุ่มตา) |
| `.input.is-invalid` | ช่องที่กรอกผิด (กรอบแดง) |
| `.pill` + `-ok/-warn/-danger/-info` | ป้ายสถานะ |
| `.flash` + `-error/-ok` | กล่องแจ้งเตือน |
| `.card` `.card-head` `.card-body` | การ์ดมาตรฐาน |
| `.tbl` ใน `.table-scroll` | ตาราง (หัวตารางติดขอบบนเวลาเลื่อน) |
| `.lang` | ตัวสลับภาษา — **ใช้ธงชาติ** (ใส่ `.on-dark` เมื่อวางบนแถบสีเข้ม) |
| `.flag` | ธงชาติ 22×15 (`object-fit: cover` กันธงสัดส่วนไม่เท่ากันบิดเบี้ยว) |
| `.sr-only` | ข้อความสำหรับ screen reader |

### ไฟล์ภาพประจำระบบ (`public/img/`)

| ไฟล์ | ใช้ที่ไหน |
|---|---|
| `bms-logo.png` | โลโก้ BMS (เจ้าของให้มา) — วางบนพื้นน้ำเงินตรงๆ **ห้ามใส่กรอบขาวรอง** |
| `flag-th.webp` | ธงไทย — ⚠️ เป็นไฟล์ WebP ไฟล์ต้นฉบับตั้งนามสกุล `.png` ไว้ผิด จึงเปลี่ยนให้ตรงชนิดจริง |
| `flag-en.png` | ธงอังกฤษ |
| `toolbar/*.png` | ไอคอนแถบเครื่องมือ (`home` `refresh` `help` — ยังเก็บ `search` `export` ไว้เผื่อใช้) ขนาด 128px พื้นหลังโปร่ง |
| `nav/*.png` | ไอคอนเมนูซ้ายทั้งหมด ขนาด 128px พื้นหลังโปร่ง · แสดงที่ 22px (หัวข้อหลัก 24px) |

**ไอคอนแถบเครื่องมือ** ตัดมาจากภาพรวมที่เจ้าของให้ (`ให้วิเคราะห์/Frontend/สัญลักษณ์ไอคอน/ไอคอน Navbar ย่อย.png`)
ทำพื้นหลังโปร่งด้วยการ flood fill จากขอบภาพ (ลบเฉพาะพื้นขาวรอบนอก **ไม่แตะสีขาวที่อยู่ในตัวไอคอน**)

เพิ่ม/ลดปุ่มในแถบเครื่องมือ ให้แก้ที่อาเรย์ `$tools` ใน [`layouts/app.blade.php`](resources/views/layouts/app.blade.php) ที่เดียว
แล้ววางไฟล์ไอคอนชื่อเดียวกันไว้ที่ `public/img/toolbar/` + เพิ่มคีย์แปลครบ 2 ภาษา

**การตัดไอคอนจากภาพต้นฉบับ — ไฟล์ต้นทางมี 3 แบบ ใช้วิธีต่างกัน**

| พื้นหลังต้นฉบับ | วิธีทำให้โปร่ง |
|---|---|
| ขาว/เทาอ่อน | flood fill จากขอบภาพ ลบเฉพาะพื้นรอบนอก **ไม่แตะสีขาวในตัวไอคอน** |
| ดำ | ถอด alpha จากค่าสีที่สว่างสุด แล้ว un-premultiply คืนสีจริง + ตัดแสงเรืองทิ้ง |
| โปร่งอยู่แล้ว | หาขอบจากค่า alpha แล้วครอบตัดได้เลย |

⚠️ ดูให้แน่ก่อนว่าไฟล์เป็นแบบไหน (`PixelFormat` + สุ่มอ่านพิกเซลมุมภาพ) ใช้วิธีผิดจะได้กล่องทึบมาแทนไอคอน

**โครงหน้า** อยู่ใน `layouts/app.blade.php` : `.topbar` · `.menubar` · `.toolbar` · `.sidebar` · `.content` · `.statusbar`

**เมนูซ้าย** แก้ที่ [`layouts/partials/sidebar.blade.php`](resources/views/layouts/partials/sidebar.blade.php) ที่เดียว (อาเรย์ `$groups`)

---

## 5. แม่แบบหน้าใหม่

```blade
@extends('layouts.app')

@section('title', 'ชื่อหน้า')

@push('page-style')
  <style>
    /* CSS เฉพาะหน้านี้ — ใช้ var() เท่านั้น ห้ามค่าดิบ */
  </style>
@endpush

@section('content')
  <div class="card">
    <div class="card-head">
      <h3 data-i18n="module.page.title">ชื่อหน้า</h3>
    </div>
    <div class="card-body">
      ...
    </div>
  </div>
@endsection

@push('page-script')
  <script>
    'use strict';
    (function () {
      // ...
    })();
  </script>
@endpush
```

---

## 6. ภาษา — ไทย + อังกฤษ ทุกจุด (โปรเจคนี้ 2 ภาษา ไม่ใช่ 3)

> 🔴 **กติกาที่เจ้าของสั่งไว้: เขียนคำอธิบายภาษาไทยเมื่อไหร่ ต้องเขียนภาษาอังกฤษให้เสมอ ทุกครั้ง ไม่ต้องรอให้สั่ง**

ครอบคลุมทุกอย่างที่ผู้ใช้มองเห็น: ปุ่ม · เมนู · หัวข้อ · label · placeholder · tooltip · error · ป้ายสถานะ · หัวตาราง · หัวคอลัมน์ในไฟล์ Export · หัวข้อและเนื้อหาอีเมล

**ไม่ต้องแปล:** โค้ด · ชื่อตัวแปร · ชื่อ field · ชื่อตาราง · SQL · commit message (comment ในโค้ดเขียนไทยได้ และควรบอก "ทำไม")

### 6.1 ข้อความคงที่ → `data-i18n`

```blade
<button data-i18n="po.create">สร้างใบขอซื้อ</button>
<th data-i18n="po.table.status">สถานะ</th>
<input data-i18n-placeholder="po.search" placeholder="ค้นหา">
<button data-i18n-aria="po.delete" aria-label="ลบรายการนี้">✕</button>
```

คำแปลอยู่ที่ [`layouts/i18n.blade.php`](resources/views/layouts/i18n.blade.php) — มี 2 บล็อก `th` และ `en`

🔴 **เพิ่มคีย์ต้องเพิ่มครบ 2 บล็อกพร้อมกันเสมอ** ถ้าเพิ่มภาษาเดียว หน้าเว็บจะโชว์ชื่อคีย์ดิบให้ผู้ใช้เห็น

⚠️ `data-i18n` เขียนทับ `textContent` ทั้งก้อน ถ้าใน element มี `<b>` หรือ `{{ $ตัวแปร }}` ปนอยู่ **จะหายหมด**
```blade
{{-- ❌ ผิด: เลขจะหายตอนสลับภาษา --}}
<b data-i18n="po.count">รายการทั้งหมด {{ $n }}</b>

{{-- ✅ ถูก --}}
<b><span data-i18n="po.count">รายการทั้งหมด</span> {{ $n }}</b>
```

### 6.2 ข้อความจากฐานข้อมูล → `data-loc-th` / `data-loc-en`

```blade
<span data-loc-th="{{ $item->name_th }}" data-loc-en="{{ $item->name_en }}">{{ $item->name_th }}</span>
```

- ตั้งชื่อคอลัมน์คู่กันเสมอ: `name_th` / `name_en` · `desc_th` / `desc_en`
- ไม่มีค่า EN ให้ **ถอยไปใช้ TH อัตโนมัติ** (ระบบทำให้แล้ว) — ห้ามปล่อยว่าง
- 🔴 **`data-loc-th` เขียนทับ textContent ทั้งก้อน** — ห้ามใส่กับ element ที่ต้องคงข้อความเดิม
  อยากได้แค่ **tooltip 2 ภาษาจากฐานข้อมูล** ให้ใช้ `data-loc-title-th` / `data-loc-title-en` (เพิ่ม 2026-09-17)
  ```blade
  {{-- ป้ายโค้ด "NM" คงเดิม · tooltip สลับชื่อกลุ่มตามภาษา --}}
  <span title="{{ $g->name_th }}" data-loc-title-th="{{ $g->name_th }}" data-loc-title-en="{{ $g->name_en }}">{{ $g->code }}</span>
  ```

### 6.3 ข้อความจากฝั่ง PHP (flash / error)

ส่งเป็นคู่ th/en เสมอ แล้วให้ view เรนเดอร์ด้วย `data-loc-*`

```php
return back()->with('flash_error', ['th' => 'บันทึกไม่สำเร็จ', 'en' => 'Could not save']);
```

### 6.4 ข้อความที่ JS สร้างตอนรัน

`data-i18n` ทำงานตอนโหลดหน้า — element ที่ JS สร้างทีหลังจะไม่ถูกแปล ให้ใช้

```js
window.BMS.t('po.create', 'สร้างใบขอซื้อ')     // อ่านคำแปลตามภาษาปัจจุบัน
document.addEventListener('bms:languagechange', function () { /* วาดใหม่ */ });
```

### 6.5 ตรวจก่อนบอกว่าเสร็จ

```bash
for k in $(cat <ไฟล์ html ที่เรนเดอร์แล้ว> | grep -ohE 'data-i18n(-placeholder|-aria|-title)?="[^"]+"' \
           | sed 's/.*="//;s/"//' | sort -u); do
  n=$(grep -c "'$k':" resources/views/layouts/i18n.blade.php)
  [ "$n" = "2" ] || echo "ขาด: $k = $n ภาษา"
done
```

ภาษาเริ่มต้นของระบบคือ **ไทย** · ภาษาที่เลือกถูกจำใน `localStorage` (คีย์ `bms_lang`)

---

## 6C. โทรศัพท์ — ทุกหน้าต้องใช้งานได้จริง (เจ้าของสั่ง 2026-09-18)

> 🔴 **กฎเต็มอยู่ที่ [`CLAUDE.md`](./CLAUDE.md) หัวข้อ "ทุกหน้า/ทุกระบบที่เพิ่มต่อจากนี้ ต้องใช้งานบนโทรศัพท์ได้"**
> หัวข้อนี้เก็บเฉพาะโค้ดที่เอาไปวางได้เลย

**จุดหักของจอมือถือในระบบนี้มี 3 ขนาด** — `≤860px` เมนูกลายเป็นลิ้นชัก · `≤560px` แถบบนยุบเหลือปุ่ม ☰ + แผงกางเต็มจอ · `≤420px` ปุ่มแถบเครื่องมือเหลือไอคอน

### ความสูงเต็มจอ

```css
/* 🔴 เขียนคู่เสมอ — vh เป็นตัวสำรอง, dvh คือค่าที่ถูกจริงบนมือถือ */
.something-fullscreen {
  height: 100vh;
  height: 100dvh;
}
```

### แผงที่ห้อยจากปุ่ม (popover)

```css
@media (max-width: 560px) {
  .my-pop {
    position: fixed; top: calc(var(--topbar-h) + 4px);
    left: 8px; right: 8px; width: auto;
    max-height: calc(100dvh - var(--topbar-h) - 16px); overflow: auto;
  }
}
```

```js
// 🔴 ปิดแผงอื่นก่อนเปิดของตัวเองเสมอ (มีตัวกลางให้แล้วที่ layouts/app)
btn.addEventListener('click', function (e) {
  e.stopPropagation();
  if (window.BMS && window.BMS.closePopovers) { window.BMS.closePopovers(myPop); }
  myPop.hidden = !myPop.hidden;
});
```

🔴 **กฎของแผงต้องเขียนไว้ในไฟล์เดียวกับที่นิยามแผงนั้น** — CSS ของ `bell` อยู่ใน body ซึ่งมา**ทีหลัง** `layouts/app`
เขียนกฎมือถือของกระดิ่งไว้ที่ `layouts/app` จะแพ้ลำดับ cascade แบบเงียบๆ

### หน้าทางตัน (403 / 404)

ใช้ [`errors/partials/page.blade.php`](resources/views/errors/partials/page.blade.php) — ส่ง `$code` · `$titleTh/$titleEn` · `$textTh/$textEn`
มีปุ่มกลับหน้าข้อมูลส่วนตัว + ออกจากระบบให้อัตโนมัติ (คนที่ยังไม่ล็อกอินได้ปุ่มเข้าสู่ระบบแทน)

---
## 6A. รูปภาพ — กฎบังคับ

### 🔴 รูปโปรไฟล์ทุกที่ ต้องกดขยายได้ และหมุนล้อเมาส์เพื่อซูมได้

เจ้าของสั่งไว้ 2026-09-02 — ใช้กับ **ทุกหน้าที่มีรูปคน** (navbar · หน้าโปรไฟล์ · รายชื่อพนักงาน ·
การ์ดผู้อนุมัติ · ตารางที่มีรูป ฯลฯ) **ห้ามมีรูปโปรไฟล์ที่กดไม่ได้**

ตัวขยายรูปทำไว้ให้แล้วที่ [`layouts/partials/image-zoom.blade.php`](resources/views/layouts/partials/image-zoom.blade.php)
`layouts/app.blade.php` include ไว้แล้ว **ทุกหน้าที่ `@extends('layouts.app')` ใช้ได้เลย ไม่ต้องเขียนใหม่**

```blade
{{-- แบบง่าย: ใส่ที่ <img> ตรงๆ --}}
<img src="{{ $user->avatarUrl() }}" data-zoomable alt="">

{{-- แบบที่ใช้จริงกับรูปโปรไฟล์: ปุ่มครอบรูปเล็ก แล้วชี้ไปรูปใหญ่ --}}
<button type="button" class="who-avatar is-btn" data-zoomable
        data-zoom-src="{{ $user->avatarUrl() }}" data-zoom-alt="{{ $user->displayName('th') }}"
        aria-label="ดูรูปโปรไฟล์ขนาดใหญ่" data-i18n-aria="top.viewPhoto">
  <img src="{{ $user->avatarUrl() }}" alt="" loading="lazy" data-avatar data-initial="{{ $user->initial() }}">
</button>
```

ได้มาให้ครบแล้ว: หมุนล้อซูม (ซูมเข้าหาตำแหน่งเคอร์เซอร์) · ลากเพื่อเลื่อน · ดับเบิลคลิกสลับขยาย ·
ปุ่ม `+` `−` `รีเซ็ต` `ปิด` · คีย์ลัด `Esc` `+` `−` `0` · คลิกพื้นหลังเพื่อปิด · คืนโฟกัสให้ปุ่มเดิมตอนปิด

### 🔴 ไอคอนเอกสารแนบ — ใช้ชุดนี้เสมอ (เจ้าของสั่ง 2026-09-03)

ตัดจาก `ให้วิเคราะห์/Frontend/สัญลักษณ์ไอคอน/ไอคอนเอกสาร.png` เก็บที่ **`public/img/file/*.png`** (10 แบบ)

`pdf` · `docx` · `xlsx` · `ppt` · `txt` · `csv` · `zip` · `jpg` · `png` · `json`

เลือกตามนามสกุลไฟล์ด้วย [`App\Support\FileIcon`](app/Support/FileIcon.php) — **ห้ามไปหาไอคอนจากที่อื่นมาปน**

```blade
<img class="file-ico" src="{{ \App\Support\FileIcon::url($file->original_name) }}" alt="">
```

ฝั่ง JS (ตอนเพิ่งเลือกไฟล์ ยังไม่อัปโหลด) ให้ controller ส่ง `FileIcon::urlMap()` เข้าหน้า
นามสกุลที่ไม่มีไอคอนของตัวเองถอยไปใช้ `txt` — **ห้ามปล่อยรูปแตก**


### รายการไฟล์แนบ — ต้องเห็นไฟล์ + กดกากบาทเอาออกได้

- เลือกไฟล์แล้ว **แสดงรายชื่อทันที** พร้อมไอคอนตามชนิดและขนาดไฟล์
- มีปุ่ม **✕** ท้ายแถวเพื่อเอาไฟล์นั้นออกก่อนบันทึก (ไม่ใส่กรอบ ตามกฎด้านล่าง)
- เลือกไฟล์รอบสอง = **ต่อท้ายของเดิม ไม่ทับ** และตัดไฟล์ชื่อ+ขนาดซ้ำออก
- 🔴 `<input type="file">` ตั้งค่ารายการไฟล์เองไม่ได้ (เบราว์เซอร์ล็อกไว้)
  ต้องเก็บไฟล์ไว้ในตัวแปรเอง แล้วยัดกลับผ่าน `DataTransfer` ทุกครั้งที่เอาไฟล์ออก


### รูปที่โหลดไม่ได้ ต้องมีตัวสำรองเสมอ

รูปโปรไฟล์เปิดจากสตอเรจของ **Insight ผ่าน HTTP** (ไม่ได้ก็อปไฟล์มา) จึงมีโอกาสโหลดไม่ติด

- ใส่ `data-avatar data-initial="{{ $user->initial() }}"` ที่ `<img>` แล้ว `layouts/app.blade.php`
  จะสลับเป็นตัวอักษรแรกของชื่อให้เอง
- 🔴 **ตอนสลับเป็นตัวอักษร ต้องถอด `data-zoomable` ออกด้วย** ไม่งั้นกดแล้วจะเปิดกล่องรูปเปล่า (ทำไว้แล้วในเลย์เอาต์)

### ปุ่มที่ต้องทำ 2 หน้าที่ ให้แยกเป็น 2 ปุ่ม

บนแถบบน **กดที่รูป = ขยายรูป · กดที่ชื่อ = เปิดเมนู** จึงต้องแยกเป็นคนละปุ่ม
ถ้ารวมเป็นปุ่มเดียวจะทำทั้ง 2 อย่างไม่ได้ — ใช้หลักนี้กับที่อื่นด้วย

---

## 6B. แถบบน (Navbar) — ลำดับที่ตายตัว

เรียงจากซ้ายไปขวา **ทุกหน้าต้องเหมือนกัน ห้ามสลับ**

```
[ BMS + ชื่อระบบ ]  ······  [ 🔔 แจ้งเตือน ] | [ รูปโปรไฟล์ ] [ ชื่อ + ตำแหน่ง ▾ ] | [ 🇹🇭 ธงภาษา ▾ ]
```

### 🔔 กระดิ่งแจ้งเตือน — [`layouts/partials/bell.blade.php`](resources/views/layouts/partials/bell.blade.php)

**เลขแดงบนกระดิ่ง** ทำแบบเฟสบุค — อยู่มุมขวาบนของไอคอน ไม่ใช่ต่อท้าย

```css
.bell-dot {
  position: absolute; top: -2px; right: -4px;
  background: #e02424; color: #fff;          /* แดง-ขาว ตายตัว */
  box-shadow: 0 0 0 2px var(--navy-900);     /* ขอบสีเดียวกับแถบบน ให้ตัวเลขลอยเด่น */
}
```

**แผงที่กางออกมี 3 ชั้น เรียงเหมือนเมนูซ้ายเป๊ะ**

```
โมดูล + ไอคอน (เลขแดง)      ← กดเพื่อกาง/พับ
  หัวข้อย่อย (เลขแดง)        ← กดแล้วไปหน้านั้น + ทำเครื่องหมายอ่านเฉพาะหัวข้อนี้
    รายการจริง (เลขที่เอกสาร + ชื่อเรื่อง + เวลา)
```

🔴 **ชั้นหัวข้อย่อยต้องอ่านจาก `NavMenu::menuFunctions()`** ตัวเดียวกับเมนูซ้าย
หัวข้อที่ยุบรวมกัน (`รับทราบ` + `อนุมัติ Invest`) ต้องรวมเลขเป็นก้อนเดียว
ไม่งั้นเลขบนกระดิ่งกับเมนูซ้ายไม่ตรงกัน ผู้ใช้กดตามแล้วหาไม่เจอ

ไม่มีแจ้งเตือน = แสดง "ยังไม่มีแจ้งเตือน" ไม่ใช่แผงว่างเปล่า

### โครงแผงกระดิ่ง — 2 หน้าจอ สลับกัน (เจ้าของสั่ง 2026-09-04)

```
หน้าที่ 1 · รายการโมดูล            หน้าที่ 2 · แจ้งเตือนของโมดูลนั้น
┌──────────────────────────┐      ┌──────────────────────────┐
│ การแจ้งเตือน   อ่านทั้งหมด │      │ ‹  งบประมาณ    อ่านทั้งหมด │
├──────────────────────────┤      ├──────────────────────────┤
│ [ไอ] งบประมาณ      3  ›  │  →   │ เสนอ Invest 1 │ รับทราบ 2 │  ← แท็บ
│ [ไอ] ใบขอซื้อ      1  ›  │      ├──────────────────────────┤
└──────────────────────────┘      │ INV-2569-000001 · ...    │
                                  │ อนุมัติแล้ว — สร้างงบ ...  │
                                  └──────────────────────────┘
```

- **หน้าที่ 1** โมดูลเรียงเป็นรายการแอป — ไอคอนในกรอบ + ชื่อ + ตัวเลขแดง + ลูกศร `›`
- **หน้าที่ 2** กดโมดูลแล้ว **สลับหน้า** (ไม่ใช่กางซ้อนลงไป) · หัวแผงเปลี่ยนเป็นชื่อโมดูล
  · มีปุ่ม **`‹` ย้อนกลับ** มุมซ้ายบน · **แท็บด้านบนบอกว่าเป็นแจ้งเตือนเรื่องอะไร**
- แท็บอ่านจาก `NavMenu::menuFunctions()` ตัวเดียวกับเมนูซ้าย — หัวข้อที่ยุบรวมกันในเมนู นับรวมเป็นแท็บเดียว
- แท็บเยอะให้เลื่อนในแถบตัวเอง (`overflow-x:auto`) หน้าเว็บห้ามเลื่อนแนวนอน
- 🔴 **Esc ครั้งแรกถอยกลับหน้ารายการโมดูล ครั้งที่สองค่อยปิดแผง** — ไม่ใช่ปิดทิ้งทันที
- 🔴 **แจ้งเตือน 1 ใบต้องมีหัวข้อกำกับทุกบรรทัด** — `เลขที่ / <ป้ายของโมดูล> / แจ้งเตือน / วันเวลา`
  ป้ายกว้างเท่ากันทุกบรรทัด ค่าจึงเรียงตรงกัน · ป้ายบรรทัดที่ 2 มาจาก `subject_key` ของแจ้งเตือนใบนั้น
  (รายละเอียดและกติกาสำหรับโมดูลใหม่อยู่ใน [`DECISIONS.md` ข้อ 24.1b](./DECISIONS.md))
- เปิดกระดิ่งใหม่ทุกครั้งเริ่มที่หน้ารายการโมดูลเสมอ

### แถบเครื่องมือ (Navbar ย่อย) — 3 ปุ่ม

| ปุ่ม | ทำอะไร |
|---|---|
| **หน้าแรก** | ลิงก์ไป `route('guide')` — หน้าคำแนะนำการใช้งาน |
| **รีเฟรช** | `location.reload()` เหมือนกดรีเฟรชของเบราว์เซอร์ |
| **ช่วยเหลือ** | เปิดกล่องเล็กแบบหน้าต่างแชท — แผนกบุคคล (`bms.hr_email`) · แผนกไอที (`bms.it_email`) |

กล่องช่วยเหลือมีแค่ **ชื่อแผนก + อีเมล** ไม่ต้องมีคำอธิบายต่อท้าย (เจ้าของสั่งตัดออก 2026-09-02)
ปิดได้ 3 ทาง: ปุ่ม `×` · กดนอกกล่อง · `Esc` — ปิดแล้วคืนโฟกัสให้ปุ่มที่เปิด

⚠️ **ห้ามใส่ `overflow` ให้ `.toolbar`** — เคยใส่ `overflow-x: auto` แล้วมันไปตัดกล่องช่วยเหลือ
ที่ลอยออกมานอกแถบหายทั้งกล่อง ของที่ยาวเกินให้จัดการในตัวมันเอง (เช่น `.crumb` ตัดด้วย ellipsis)

**ปุ่ม `«` ถัดจากปุ่มสุดท้าย = ยุบปุ่มไปทางซ้าย** — ยุบแล้วปุ่มทั้ง 3 หายไป เหลือแค่ปุ่มกดกลับ (กลับหัวเป็น `»`)
ความสูงแถวเท่าเดิม 🔴 **ห้ามซ่อนทั้งแถว** เพราะแถวนี้ใช้ร่วมกับหัวเมนูซ้ายและ breadcrumb

เพิ่ม/ลดปุ่มให้แก้ในแถบ `.toolbar` ของ [`layouts/app.blade.php`](resources/views/layouts/app.blade.php)
แล้ววางไฟล์ไอคอนที่ `public/img/toolbar/` + เพิ่มคีย์แปลครบ 2 ภาษา

### เส้นทางของหน้า (breadcrumb) — อยู่ขวาสุดของแถบเครื่องมือ

```
หน้าแรก › ตั้งค่าระบบ › บัญชีผู้ใช้ภายนอก (Supplier)
```

- ข้อแรก `หน้าแรก` ระบบใส่ให้เองทุกหน้า (ลิงก์ไป `/guide`) ไม่ต้องเขียนซ้ำ
- ข้อสุดท้าย = หน้าที่กำลังดูอยู่ **ไม่ทำเป็นลิงก์** (ใส่ `aria-current="page"`)
- ข้อกลางที่ยังไม่มีหน้าจริง ให้เป็นข้อความเฉยๆ ไม่ใช่ลิงก์ตาย
- ลิงก์ **เปลี่ยนสีเป็น `--accent` + ขีดเส้นใต้ตอนเอาเมาส์ชี้**

🔴 **หน้าใหม่ทุกหน้าต้องเพิ่มเส้นทางของตัวเองที่ `$trails`** ใน
[`layouts/partials/breadcrumb.blade.php`](resources/views/layouts/partials/breadcrumb.blade.php)
ไม่งั้นจะขึ้นแค่ "หน้าแรก" แล้วผู้ใช้ไม่รู้ว่าอยู่ตรงไหน

### เมนูซ้าย — โครง 3 ชั้น และความหมายของไอคอน

```
หัวข้อหลัก           icon: group / dashboard / settings
  └─ โมดูลย่อย        icon: module     เช่น งบประมาณ · ใบขอซื้อ
       └─ ฟังก์ชัน     icon: file       เช่น สร้างใบขอซื้อ · รายการรออนุมัติ
```

🔴 **ใช้ไอคอนให้ตรงความหมายเสมอ** (เจ้าของกำหนด 2026-09-02) — ไฟล์อยู่ที่ `public/img/nav/`

| ชื่อที่เรียกในโค้ด | ใช้กับ |
|---|---|
| `mod-dashboard` | หัวข้อหลัก **แดชบอร์ด** (กราฟแท่ง) |
| `dashboard` | หน้า **ภาพรวมระบบ** (โฟลเดอร์แดชบอร์ด) |
| `group` | หัวข้อหลักทั่วไป เช่น **โมดูลระบบงาน** (โฟลเดอร์เปล่า) |
| `module` | โมดูลย่อย (โฟลเดอร์มีเอกสาร) |
| `file` | ฟังก์ชัน/หน้าจอในโมดูลย่อย |
| `settings-main` | หัวข้อหลัก **ตั้งค่าระบบ** |
| `settings-sub` | **ทุกหน้าย่อยใต้ตั้งค่าระบบ** |
| `mod-budget` `mod-pr` `mod-pr-review` `mod-rfq` `mod-spec` `mod-quote`<br>`mod-po-invoice` `mod-receiving` `mod-payment` `mod-tax` `mod-report` `mod-master` | หน้าในโมดูลระบบงาน — ไอคอนตรงตามขั้นตอนจัดซื้อ (12 โมดูล)<br>🔴 รายชื่อและลำดับเต็มอยู่ที่ [`DECISIONS.md` ข้อ 1](DECISIONS.md#1-ขอบเขตโปรเจค) · โค้ดอยู่ที่ `app/Support/NavMenu.php` |

โครงเมนูปัจจุบัน (แก้ที่ [`layouts/partials/sidebar.blade.php`](resources/views/layouts/partials/sidebar.blade.php) ที่เดียว):

```
แดชบอร์ด        ภาพรวมระบบ

โมดูลระบบงาน     เรียงตามลำดับงานจริง ไม่ได้เรียงตามตัวอักษร
                ตั้งงบประมาณ → สร้างใบขอซื้อ → ตรวจสอบใบขอซื้อ → เปรียบเทียบราคาผู้ขาย
                → รับและตรวจรับสินค้า → ใบสั่งซื้อและใบแจ้งหนี้ → ตั้งเบิกและจ่ายเงิน
                → ภาษีหัก ณ ที่จ่าย

ตั้งค่าระบบ       สิทธิ์การเข้าถึงโมดูล · ข้อมูลหลักของระบบ
                ประวัติการใช้งานระบบ · บัญชีผู้ใช้ภายนอก (Supplier)
```

🔴 **เมนูโมดูลเรียงตามลำดับงานจริง ห้ามเรียงใหม่ตามตัวอักษร** — ผู้ใช้ไล่สายตาตามขั้นตอนที่ทำจริง

### แผงเมนูย่อยของโมดูล (เจ้าของสั่ง 2026-09-02)

**กดโมดูลในเมนูซ้าย → แผงกางออกมาทางขวา แล้วดันแถบเครื่องมือกับ breadcrumb ไปด้วย**
(วัดจริง: แถบเครื่องมือเลื่อนจาก x=232 → 458 ตอนเปิดแผง)

- ชื่อโมดูลตั้งให้ **กว้างๆ ครอบคลุมงานทั้งก้อน** (เช่น `งบประมาณ` ไม่ใช่ `ตั้งงบประมาณ`)
  รายละเอียดของงานไปอยู่ในแผงย่อยแทน
- หัวแผง **พื้นน้ำเงินเข้ม** + ไอคอนโมดูล + ปุ่ม `«` ปิด
- ฟังก์ชันในแผงใช้ไอคอน `file` ทั้งหมด
- กดโมดูลอื่น = สลับแผง · กดโมดูลเดิมซ้ำ = ปิดแผง

🔴 **โครงเมนูทั้งหมดอยู่ที่ [`app/Support/NavMenu.php`](app/Support/NavMenu.php) ที่เดียว**
ทั้งเมนูซ้ายและแผงย่อยอ่านจากไฟล์นี้ — แก้ที่เดียวเปลี่ยนทั้งคู่ ไม่มีทางหลุดจากกัน

### 🔴 เมนูต้องแสดงตามสิทธิ์ 3 แบบ (เจ้าของสั่ง 2026-09-03)

| สถานะ | หน้าตา | ทำยังไง |
|---|---|---|
| 🟢 **มีสิทธิ์** | แสดงปกติ กดได้ | ไม่ต้องทำอะไรเพิ่ม |
| ⚪ **ไม่มีสิทธิ์** | เทาลง · กดไม่ติด · มี tooltip *"คุณไม่มีสิทธิ์เข้าใช้งานโมดูลนี้"* · มีรูปแม่กุญแจ | `class="nav-item is-denied"` + `disabled` + `data-i18n-title="nav.noAccess"` |
| 🔒 **เมนูของผู้ดูแลระบบ** | **ซ่อนไปเลย** ไม่ให้รู้ว่ามีอยู่ | กลุ่มที่ตั้ง `'admin' => true` ใน `NavMenu` จะไม่ถูกวาดถ้าไม่ใช่แอดมิน |

**ทำไมโมดูลระบบงานถึงเทาแทนที่จะซ่อน** — เจ้าของอยากให้ผู้ใช้เห็นว่าระบบมีอะไรบ้าง จะได้ไปขอสิทธิ์ถูกที่
ส่วนเมนูจัดการระบบไม่ต้องให้รู้ว่ามีอยู่ เพราะไม่ใช่เรื่องที่ผู้ใช้ทั่วไปต้องไปขอ

**อย่าพึ่งสีอย่างเดียว** — ต้องมีรูปแม่กุญแจกำกับด้วยเสมอ คนตาบอดสีจะได้แยกออก (ดูหัวข้อ "การเข้าถึง")

**โครงสิทธิ์ 2 ชั้น** — เข้าระบบได้ไหมคุมด้วย **ตำแหน่ง** · เห็นโมดูลไหนคุมด้วย **รายชื่อคน**

ตัวตัดสินสิทธิ์อยู่ที่ [`App\Services\Access\AccessService`](app/Services/Access/AccessService.php) ที่เดียว
🔴 **ห้ามเขียนเงื่อนไข `role === 'admin'` เองในหน้าใดๆ** ให้เรียก `AccessService::isAdmin()` เสมอ
เพราะแอดมินมี 2 ทาง: role ที่มิเรอร์มาจาก Insight และตาราง `access_admins` ของ BMS เอง

### รูปพนักงานในตาราง

ใช้ [`resources/views/access/partials/avatar.blade.php`](resources/views/access/partials/avatar.blade.php) เป็นแบบอ้างอิง

- รูปมาจาก 2 ที่ — `app_users.profile_picture` ก่อน ไม่มีค่อยถอยไป `employees.photo_path`
- **ต้องกดขยายได้และหมุนล้อเมาส์ซูมได้เสมอ** (`data-zoomable` + `data-zoom-src`)
- ไม่มีรูป → โชว์ตัวอักษรแรกของชื่อ และ**ไม่ต้องให้กด** เพราะไม่มีอะไรให้ดู
- รูปโหลดไม่ได้ → `onerror` เปลี่ยนกลับเป็นตัวอักษรแรก


### แผงเมนูย่อย — เปิดค้างเมื่อไหร่ ปิดเมื่อไหร่ (เจ้าของสั่ง 2026-09-03)

| สถานการณ์ | ผลที่ต้องได้ |
|---|---|
| กดหน้าที่อยู่ในแผงนั้น (สิทธิ์การเข้าถึง → สิทธิ์การเข้าถึงระบบ BMS) | **แผงค้างไว้** |
| กดเมนูอื่นที่ไม่มีแผงของตัวเอง (แดชบอร์ด · ข้อมูลหลัก) | **ปิดแผงทิ้ง** |
| ผู้ใช้กดปุ่ม `«` เอง | ปิดแผงทิ้ง |

🔴 **ห้ามจำ id ที่เปิดล่าสุดไว้ใน `localStorage`** — เคยทำแล้วแผงตามไปโผล่ในหน้าที่ไม่เกี่ยวข้อง (บั๊กจริง 2026-09-03)
สถานะที่ถูกต้องอ่านจากหน้าปัจจุบันได้อยู่แล้ว: มี `.submenu-item.is-active` อยู่ในแผงไหน ก็เปิดแผงนั้น ไม่มีก็ไม่ต้องเปิด
- 🔴 **มีอนิเมชันเลื่อนเข้า-ออก** ทั้งแผงเมนูย่อยและการกาง/พับหัวข้อหลักในเมนูซ้าย
- 🔴 **ตัวกรองของแผงย่อยต้องตรงกับเมนูซ้ายเป๊ะๆ** — เคย hardcode `id === 'access'` ไว้ พอเพิ่มโมดูลใหม่ในกลุ่มแอดมิน
  แผงไม่ถูกวาดเลย กดแล้วไม่มีอะไรขึ้น (บั๊กจริง 2026-09-03) · ให้ใช้ `NavMenu::adminModuleIds()` แทนการไล่เช็คทีละ id
- **เมนูที่เนื้อหาไม่เยอะพอ ไม่ต้องมีแผงย่อย** — ทำเป็นลิงก์ตรงหน้าเดียวแล้วแยกด้วยแท็บในหน้าเอง
  (ข้อมูลหลัก · ประวัติการใช้งานระบบ ใช้แบบนี้)
- ตอนกู้สถานะคืนตอนโหลดหน้า ให้ใส่คลาส `.no-anim` ชั่วคราว **ไม่งั้นแผงจะวูบทุกครั้งที่เปลี่ยนหน้า**
- ห้ามใช้ `display: none` กับสิ่งที่ต้องมีอนิเมชัน — `display` เปลี่ยนแบบทันที ทรานซิชันจะไม่ทำงาน
  ใช้ `visibility` + `opacity` + `transform` แทน
- `<details>` เปลี่ยนแบบทันทีเช่นกัน ต้องคุม `height` ด้วย JS ถึงจะเห็นการเคลื่อนไหว
- ทุกอนิเมชันต้องปิดให้ผู้ใช้ที่ตั้ง `prefers-reduced-motion: reduce`

### ตารางที่มีปุ่ม "แก้ไขสิทธิ์" — แม่แบบที่ใช้ซ้ำได้

ดูของจริงที่ [`resources/views/access/system.blade.php`](resources/views/access/system.blade.php)

- ตารางหลัก 4 คอลัมน์: **ชื่อสิ่งที่คุม · ผู้ที่ได้รับ · จำนวน · จัดการ**
- ปุ่ม `แก้ไขสิทธิ์` กางแถวแผงออกมาใต้แถวนั้น (`<tr class="editor" hidden>`) — **เปิดได้ทีละแผง**
- 🔴 รายการในคอลัมน์ที่ยาวเกินให้โชว์แค่ 6–8 รายการแรก ที่เหลือซ่อนแล้วมีปุ่ม **"รายละเอียด"**
- ตารางต้องเลื่อนในกรอบตัวเอง (`.tbl-wrap { overflow-x: auto }`) · `min-width` ต้องพอดีกับตอน**กางแผงเมนูย่อยแล้ว** ไม่งั้นคอลัมน์ "จัดการ" จะโดนตัด

### ตัวเลือกพนักงาน (people picker)

ใช้ซ้ำได้ทันที: [`access/partials/people-picker.blade.php`](resources/views/access/partials/people-picker.blade.php) + [`picker-assets.blade.php`](resources/views/access/partials/picker-assets.blade.php)

```blade
@include('access.partials.people-picker', [
  'pickerId' => 'ชื่อไม่ซ้ำในหน้านี้',
  'field'    => 'employee_codes[]',
  'selected' => $people,   // จาก AccessService::describeCodes()
])
@include('access.partials.picker-assets')   {{-- ใส่ครั้งเดียวต่อหน้า --}}
```

- ค้นด้วย **รหัสพนักงาน หรือ ชื่อ-สกุล** · หน่วง 260ms ก่อนยิง · ต้องพิมพ์อย่างน้อย 2 ตัวอักษร
- 🔴 **แถวผลค้นหาต้องเป็น `<div>` ไม่ใช่ `<button>`** เพราะข้างในมี 2 ปุ่มซ้อน (ดูรูป / เลือกคน)
  ซ้อน `<button>` ใน `<button>` เป็น HTML ที่ไม่ถูกต้อง เบราว์เซอร์จะแยกกล่องออกจากกันเอง
- ชื่อที่ JS สร้างขึ้นต้องเรียก `window.BMS.refresh()` ต่อท้ายเสมอ ไม่งั้นสลับภาษาแล้วชื่อไม่เปลี่ยน


### 🔴 อย่าใส่กรอบให้สิ่งที่ไม่ต้องมีกรอบ (เจ้าของสั่ง 2026-09-03)

> _"รูปโปรไฟล์ฉันเห็นคุณใส่กรอบ ไม่ต้องใส่กรอบ และในส่วนสถานะต่างๆที่มีกรอบข้อความอ่ะ ไม่ต้องใส่กรอบนะ (มันคล้าย AI)"_

| สิ่งที่ทำ | ห้าม | ให้ทำแทน |
|---|---|---|
| รูปโปรไฟล์ / avatar | `border` · วงแหวน `box-shadow` | รูปกลมเปล่าๆ พื้นหลังอ่อนตอนไม่มีรูป |
| ป้ายสถานะ (`ทุกคนที่เข้าระบบได้`, `จาก Insight`) | กล่องพื้นสี + ขอบมน | **ข้อความสีล้วน** `.state` / `.state-all` / `.state-off` / `.state-muted` |
| รายการหลายค่า (ตำแหน่ง, ชื่อคน) | ป้ายกรอบเรียงกัน | ข้อความคั่นด้วย `·` |
| ปุ่มรอง เช่น "รายละเอียด" | ปุ่มมีกรอบ | `.link-btn` — ข้อความขีดเส้นใต้ |
| ช่องติ๊กในรายการยาว | การ์ดมีกรอบต่อ 1 รายการ | เส้นคั่นล่างบางๆ |

**ข้อความอธิบายก็ต้องสั้น** — เจ้าของบอกว่า _"ตัวอักษรบอกมากเกินไป"_
หัวข้อ + ตารางควรอธิบายตัวเองได้ **อย่าใส่ย่อหน้าอธิบายยาวๆ ไว้บนตาราง**

### หน้าต่างซ้อน (modal) — ใช้กับการแก้ไขและดูรายละเอียด

- 🔴 **ปุ่ม "แก้ไขสิทธิ์" และ "รายละเอียด" ระดับโมดูล ต้องเปิด modal** ไม่ใช่กางแถวในตาราง
  (ยกเว้น **หัวข้อย่อย** ที่เจ้าของสั่งให้กางเป็นแถวในตารางแทน 2026-09-03 — ดูหัวข้อ "สิทธิ์ของหัวข้อย่อย")
- 🔴 **เลือกคนในผลค้นหาแล้ว modal ต้องค้างอยู่** ให้เลือกคนต่อไปได้เลย ห้ามปิดเอง
  → ตัวปิดเมื่อคลิกพื้นหลังต้องเช็ค `e.target.classList.contains('modal-wrap')` เท่านั้น ไม่ใช่ `!modal.contains(target)`
- `Esc` ครั้งแรกปิดแค่รายการผลค้นหา ครั้งที่สองค่อยปิดหน้าต่าง
- ปุ่มที่มีทั้ง `data-modal-close` + `data-modal-open` = ย้ายจากหน้าต่างหนึ่งไปอีกหน้าต่าง
- ชั้นการซ้อน: modal ใช้ `--z-drawer` (40) · ตัวขยายรูปใช้ `--z-modal` (60) **จะได้ซูมรูปจากในหน้าต่างได้**
- 🔴 `.modal-body` มี `overflow: auto` — **รายการผลค้นหาห้ามลอยแบบ `position: absolute`** ไม่งั้นโดนกรอบตัดทิ้ง
  ให้อยู่ในสายเนื้อหาปกติแล้วใส่ `max-height` + `overflow-y: auto` ของตัวเอง
- `<form>` ที่ครอบทั้ง head/body/foot ให้ใช้ `style="display:contents"` เพื่อไม่ให้พังโครง flex ของ modal
- **ห้ามซ้อน `<form>` ใน `<form>`** — ปุ่มลบที่อยู่ในฟอร์มเพิ่ม ให้วางฟอร์มลบไว้นอกแล้วเรียก `requestSubmit()`
- 🔴 **ปุ่มยินยอมอยู่ซ้าย ปุ่มยกเลิกอยู่ขวา เสมอ** (เจ้าของสั่ง 2026-09-03) — ใช้กับทุก `.modal-foot` และทุกหน้าต่างในระบบ
- 🔴 **หน้าต่างที่ต้องกรอกหลายรอบ กดบันทึกแล้วห้ามปิด** (เจ้าของสั่ง 2026-09-04)
  เช่นหน้าต่างเลือกคนที่ `/access/modules` — เพิ่มคนทีละคนต้องไม่ต้องเปิดหน้าต่างใหม่ทุกครั้ง
  วิธีทำ: ยิงฟอร์มด้วย `fetch` แล้วให้ controller ตอบ JSON เมื่อ `$request->expectsJson()`
  จากนั้นวาดส่วนที่เปลี่ยนในหน้าใหม่เอง · ปุ่มท้ายหน้าต่างใช้คำว่า **"ปิด"** ไม่ใช่ "ยกเลิก"
  ระหว่างบันทึกให้ปิดปุ่มกันกดซ้ำ · สำเร็จเด้ง toast · ล้มเหลวเด้งหน้าต่างค้างไว้ให้อ่าน
  🔴 token อ่านจาก `<meta name="csrf-token">` ที่ layout กลาง (มีให้แล้วทุกหน้า)


### 🔴 ทุก Action ต้องเด้งหน้าต่างแจ้งกลางจอ พร้อมอนิเมชัน (เจ้าของสั่ง 2026-09-03)

กดบันทึก · ส่ง · ลบ · ถอดสิทธิ์ · ตีกลับ — **ทุกอย่างต้องมีหน้าต่างกลางจอ**
🔴 **ห้ามใช้ `alert()` / `confirm()` / `prompt()` ของเบราว์เซอร์** — หน้าตาไม่ตรงกับระบบ และสลับลำดับปุ่มไม่ได้

| ชนิด | เครื่องหมาย | ใช้ตอน |
|---|---|---|
| `ok` | ✓ ขีดถูก | ทำสำเร็จ |
| `warn` | ! ตกใจ | ถามยืนยันก่อนทำ · เตือนกรอกไม่ครบ |
| `no` | ✕ กากบาท | ลบ · ตีกลับ · ทำไม่สำเร็จ |

- เครื่องหมาย **วาดทีละเส้น** ด้วย SVG `stroke-dashoffset` ไม่ใช่เด้งขึ้นทั้งอัน
- **ข้อความสั้น กระชับ** — หัวข้อ 1 บรรทัด คำอธิบาย 1 บรรทัด
- แจ้งผลเฉยๆ ปิดเองใน 1.8 วินาที · ข้อผิดพลาดค้างไว้จนกดปิด

ตัวจริงอยู่ที่ [`layouts/partials/dialog.blade.php`](resources/views/layouts/partials/dialog.blade.php)
**ทุกหน้าที่ `@extends('layouts.app')` ใช้ได้เลย ไม่ต้อง include ซ้ำ**

```js
window.BMS.confirm({ kind: 'no', title: 'ลบไฟล์แนบนี้?', text: '...', ok: 'ลบ',
                     onOk: function () { form.requestSubmit(); } });

window.BMS.notify({ kind: 'ok', title: 'บันทึกแล้ว' });   // ไม่มีปุ่มยกเลิก · ปิดเอง
```

ปุ่มธรรมดาใส่ attribute ได้เลย ไม่ต้องเขียน JS:

```html
<button data-confirm-th="ลบรายการ?" data-confirm-en="Delete?" data-confirm-kind="no">ลบ</button>
```

🔴 `flash_success` / `flash_error` จาก controller **เด้งหน้าต่างให้เองอัตโนมัติ** ไม่ต้องเขียนอะไรเพิ่ม


### 🔴 อย่าตั้งชื่อคลาสชนกับของที่ layout ใช้อยู่แล้ว

**เกิดขึ้นจริง 2026-09-03** — หน้ารายละเอียดสิทธิ์ ชื่อคนกลายเป็น **น้ำเงินจางจนอ่านไม่ออก**

สาเหตุ: [`layouts/app.blade.php`](resources/views/layouts/app.blade.php) มีกฎของ **แถบบน (พื้นน้ำเงินเข้ม)** อยู่แล้ว

```css
.who-text span { color: var(--on-dark-soft); }   /* สีสำหรับพื้นเข้ม */
```

พอหน้าใหม่ตั้งชื่อ `.who-text` / `.who-name` ซ้ำ กฎนั้นเลยไหลมาโดน:
`.who-text span` = **(0,1,1)** ชนะ `.who-name` = **(0,1,0)** → สีสำหรับพื้นเข้มไปอยู่บนพื้นขาว

**วิธีแก้ที่ถูก: เปลี่ยนชื่อคลาสให้ไม่ชน** (ที่ทำจริงคือ `.who-*` → `.person-*`)
🔴 **ห้ามแก้ด้วยการเติม `!important` หรือไล่เพิ่ม specificity** — เดี๋ยวก็ชนที่อื่นอีก

**เช็คก่อนตั้งชื่อคลาสใหม่ทุกครั้ง**

```bash
grep -rn "\.ชื่อคลาสที่จะใช้" resources/views/layouts/
```

ชื่อที่ layout จองไว้แล้ว (อย่าเอาไปใช้ซ้ำในหน้าเนื้อหา): `.who-text` · `.nav-item` · `.nav-icon` · `.nav-label` · `.submenu-item` · `.crumb`


### ตัวกรองในตาราง — วางไว้ที่คอลัมน์ (เจ้าของสั่ง 2026-09-03)

ดูของจริงที่ [`resources/views/audit/index.blade.php`](resources/views/audit/index.blade.php)

- ช่องกรองอยู่ใน **แถวใต้หัวตาราง** ตรงกับคอลัมน์ที่มันกรอง ไม่ใช่แถบลอยอยู่เหนือตาราง
- 🔴 **ทุกช่องต้องสูงเท่ากันและกว้างเต็มคอลัมน์** — คุมด้วยคลาสเดียว (`.fin { width:100%; height:32px }`)
  อย่าตั้งความกว้างรายช่อง ไม่งั้นช่องค้นหาจะใหญ่กว่าช่องเลือกจนดูไม่เป็นชุดเดียวกัน
- 🔴 **วาง `<form>` คร่อม `<tr>` ตรงๆ ไม่ได้** — เป็น HTML ที่ไม่ถูกต้อง เบราว์เซอร์จะดึงฟอร์มออกจากตาราง
  ให้วางฟอร์มเปล่าไว้นอกตารางแล้วให้ช่องกรองอ้างถึงด้วย attribute `form="ไอดีฟอร์ม"`
  วิธีนี้กด Enter ในช่องไหนก็ส่งฟอร์มได้ตามปกติ
- ส่งเป็น **GET** เสมอ จะได้แชร์ลิงก์ผลการกรองให้คนอื่นเปิดต่อได้
- ค่าที่ผู้ใช้พิมพ์ต้อง **หนี `%` และ `_`** ก่อนเอาไปทำ LIKE ไม่งั้นพิมพ์ `%` แล้วกลายเป็น "อะไรก็ได้"
- `min-width` ของตารางต้องพอดีกับพื้นที่จริง **ตอนกางแผงเมนูย่อยแล้ว** ไม่งั้นช่องกรองคอลัมน์สุดท้ายโดนตัด


### 🔴 สิทธิ์ของหัวข้อย่อย กำหนดที่หน้าเดียว — `/access/modules`

> **จำไว้เสมอ (เจ้าของสั่ง 2026-09-03)**
> หัวข้อย่อยของทุกโมดูล (เสนอ Invest · แจ้งขอซื้อ · Quote Compare · ฯลฯ)
> **กำหนดสิทธิ์ที่หน้า "สิทธิ์การเข้าถึงโมดูล" คอลัมน์ "หัวข้อย่อย" เท่านั้น**
> ห้ามไปเขียนเงื่อนไขสิทธิ์แยกในโมดูลใครโมดูลมัน

**วิธีใช้งาน** (เจ้าของสั่งเปลี่ยนจาก Modal เป็นแถวในตาราง 2026-09-03)

```
/access/modules  →  กดปุ่ม "V" หน้าชื่อโมดูล  →  หัวข้อย่อยกางออกเป็นแถวในตาราง (ย่อหน้า + มีไอคอน)
                 →  กด "แก้ไขสิทธิ์" ที่แถวหัวข้อย่อย  →  เลือกจากพนักงานทั้งหมด (รูปกดขยายได้)
```

- ปุ่ม **"V"** อยู่ทางซ้ายของชื่อโมดูล · โมดูลที่ไม่มีหัวข้อย่อยจะไม่มีปุ่มนี้
- แถวหัวข้อย่อย **ย่อหน้าเข้ามา** มีไอคอนของตัวเอง และแสดงรูปพนักงานที่มีสิทธิ์ (กดซูมได้เหมือนแถวหลัก)
- 🔴 **ไม่ใส่อนิเมชันความสูงกับ `<tr>`** — `display: table-row` ทำ transition ความสูงไม่ได้ ใช้จางเข้าไล่ทีละแถวแทน

**สิทธิ์เหลือ 2 ชั้น** (เจ้าของสั่งยุบจาก 3 เหลือ 2 เมื่อ 2026-09-03)

| ชั้น | คุมด้วยอะไร | ตาราง | กำหนดที่ไหน |
|---|---|---|---|
| 1. เข้าระบบได้ไหม | **ตำแหน่ง** (คนเป็นพัน คุมทีละคนไม่ไหว) | `access_login_positions` | `/access/system` |
| 2. ใช้หัวข้อย่อยไหน | **รายชื่อคน** | `access_function_users` | `/access/modules` → กดปุ่ม **"V"** แล้วแก้ที่แถวหัวข้อย่อย |

🔴 **ไม่มีสิทธิ์ระดับโมดูลให้ตั้งเองแล้ว** — "เห็นโมดูลไหม" **คำนวณจากหัวข้อย่อย**
ใช้ได้อย่างน้อย 1 หัวข้อ = เห็นโมดูล · ใช้ไม่ได้เลยสักหัวข้อ = ซ่อนโมดูล
แถวหัวข้อหลักในตารางจึงเป็น **แถวสรุป** ไม่มีปุ่มแก้ไขสิทธิ์

**เหตุผลที่ยุบ:** เดิมตั้ง 2 ที่แล้วขัดกันเอง — ให้สิทธิ์เห็นโมดูลแต่ใช้หัวข้อย่อยไม่ได้
หรือให้สิทธิ์หัวข้อย่อยแต่ไม่เห็นโมดูล แล้วสิทธิ์นั้นเงียบหายไปเฉยๆ

🔴 **สิทธิ์ต้องกันจริงฝั่งเซิร์ฟเวอร์ด้วย ไม่ใช่แค่ทำเมนูเทา**
แปะ middleware **`bms.fn`** ที่ route group ของทุกโมดูล แล้วผูก route กับหัวข้อย่อยใน `NavMenu`
(บทเรียนจริง 2026-09-03: เดิมทำแค่เมนูเทา พิมพ์ URL ตรงๆ เข้าได้หมด)

**หน้าเดียวเปิดให้หลายบทบาทได้** — ใส่คีย์ `menu` ให้หัวข้อย่อยที่ชี้หน้าเดียวกัน
`/access/modules` จะเห็นเป็นหลายแถว (ตั้งสิทธิ์แยกกัน) แต่เมนูซ้ายยุบเหลือรายการเดียว
เช่น **รับทราบ** (เห็นเฉยๆ) กับ **อนุมัติ Invest** (เซ็นได้) รวมเป็นเมนู "รับทราบ / อนุมัติ Invest"

**กติกาค่าเริ่มต้น (เหมือนกันทุกชั้น)**
🔴 **ไม่ระบุใครเลย = เปิดให้ทุกคนในชั้นบน** — ไม่ใช่ปิดหมด
ถ้าทำกลับกัน พอ migrate เสร็จทุกเมนูจะถูกล็อกทันทีจนไม่มีใครทำงานได้

**หน้าตาเมื่อไม่มีสิทธิ์** — เหมือนระดับโมดูล: เทาลง กดไม่ได้ มี tooltip บอกเหตุผล
ตัวตัดสินอยู่ที่ `AccessService::canUseFunction()` **ที่เดียว**


### 🔴 หน้าที่เป็น "ตัวเอกสาร" ต้องจัดแบบกระดาษ (เจ้าของสั่ง 2026-09-03)

ครอบคลุมทุกหน้าที่เป็นเอกสารจริง: สร้าง Invest · ดูเอกสาร · เซ็นอนุมัติ
**ไม่ใช่การ์ดฟอร์มธรรมดา** ต้องดูเหมือนกระดาษที่พิมพ์ออกมาได้

| ส่วน | กติกา |
|---|---|
| **หัวกระดาษ** | โลโก้บริษัท `public/img/company-logo.png` + ชื่อบริษัท 2 ภาษา + เลขที่เอกสาร + สถานะ · เส้นคั่นหนาใต้หัว |
| **ชื่อเรื่อง** | กลางกระดาษ ตัวหนา |
| **เนื้อเอกสาร** | ป้ายซ้าย 190px / ค่าขวา · คั่นด้วยเส้นประ (`.paper-row` / `.paper-field`) |
| **ช่องลงชื่อ** | ท้ายกระดาษเสมอ (`.sign-grid`) · **ลายเซ็นประทับลงในช่องตอนกดอนุมัติ** |
| ยังไม่เซ็น | เว้นช่องว่างไว้เฉยๆ **ไม่ใส่ข้อความ "รอลงนาม" ให้รก** |
| **ผู้รับทราบ** | 🔴 ไม่มีช่องเซ็น — ขึ้นเป็นรายชื่อใต้หัวข้อ "สำเนาเรียน (เพื่อทราบ)" |

```blade
@include('budget.partials.paper-head', ['docNo' => ..., 'status' => [...]])
  ...เนื้อเอกสาร...
@include('budget.partials.paper')   {{-- สไตล์ ใส่ท้ายหน้า --}}
```

🔴 **ลายเซ็นเก็บเป็นสำเนา ณ ตอนเซ็น** ไม่ join สดทีหลัง — คนลาออกแล้วเอกสารเก่าต้องยังพิมพ์ได้เหมือนเดิม


### คอลัมน์ "สถานะทั้งหมด" — เส้นทางเอกสาร (เจ้าของสั่ง 2026-09-03)

ทุกตารางที่มีเอกสารต้องมีคอลัมน์นี้ กดแล้วเด้งหน้าต่างบอกว่าเอกสารเดินไปถึงไหนแล้ว
ยึดแนวเดียวกับ QuoteCompare — ใช้สีบอกสถานะ

| สี | token | หมายความว่า |
|---|---|---|
| 🟢 เขียว | `--ok` | ผ่านด่านนี้แล้ว |
| 🟡 เหลือง | `--warn` | กำลังดำเนินการ (จุดกะพริบเบาๆ) |
| 🔴 แดง | `--danger` | ถูกตีกลับ |
| ⚪ เทา | `--surface-2` | ยังไม่ถึงด่านนี้ |

ชิ้นส่วน [`budget/partials/timeline.blade.php`](resources/views/budget/partials/timeline.blade.php)
แต่ละด่านบอกด้วยว่า **ใครรับทราบ ใครเซ็น** พร้อมรูปโปรไฟล์ (กดขยายได้)

🔴 **ดึงรูปพนักงานรวดเดียวแล้วส่งเป็นแผนที่ให้ view** — ไม่งั้นตาราง 20 แถวยิงคิวรีเป็นร้อยครั้ง


### 🔴 ทุกตารางต้องมีตัวกรองรายคอลัมน์แบบ Excel (เจ้าของสั่ง 2026-09-03)

ชิ้นส่วนกลาง [`layouts/partials/table-filter.blade.php`](resources/views/layouts/partials/table-filter.blade.php)
include ไว้ที่ layout แล้ว — **ทุกหน้าได้ไปด้วย ไม่ต้อง include ซ้ำ**

```blade
<table class="tbl" data-filterable>        {{-- เติมแถวช่องกรองใต้หัวตารางให้เอง --}}
  <th class="col-act" data-no-filter></th> {{-- คอลัมน์ปุ่ม/รูป ไม่ต้องมีช่องกรอง --}}
```

**หน้าตา — ถอดแบบ Excel (เจ้าของสั่ง 2026-09-03)**

- ปุ่ม **▼ สามเหลี่ยม** ต่อท้ายชื่อคอลัมน์ (ไม่ใช่ช่องค้นหาแปะไว้ใต้หัว)
- กดแล้วกางแผงเลือกค่า: ช่องค้นหา + รายการค่าที่ไม่ซ้ำแบบติ๊กได้หลายค่า + ปุ่ม "เลือกทั้งหมด" / "ล้าง"
- รายการค่าคิดจาก **แถวที่ผ่านตัวกรองคอลัมน์อื่นแล้ว** เหมือน Excel — เลือกแผนกแล้วรายการหมวดจะเหลือเฉพาะที่มีจริง
- ติ๊กครบทุกค่า = ไม่ได้กรองคอลัมน์นั้น · คอลัมน์ที่กรองค้างอยู่ ปุ่ม ▼ เปลี่ยนเป็นสีเขียว
- มีบรรทัดสรุป "แสดง X จาก Y แถว" ใต้ตาราง
- 🔴 **ตารางที่แบ่งหน้ากรองเฉพาะแถวในหน้านั้น** — ต้องกรองข้ามหน้าให้ใช้ตัวกรองฝั่งเซิร์ฟเวอร์
- 🔴 **ตารางที่มีแถวกาง/พับเอง ห้ามใส่** (เช่น `/access/modules`) ตัวกรองจะไปแหย่สถานะซ่อนของแถวย่อย


### 🔴 ตารางทั้งระบบ — หัวน้ำเงินตัวอักษรขาว · ขีดเส้นคอลัมน์ · จัดกึ่งกลาง

กำหนดไว้ที่ `.tbl th` / `.tbl td` ใน [`theme.blade.php`](resources/views/layouts/theme.blade.php) จุดเดียว
**ห้ามเขียนสี/เส้น/การจัดวางของตารางเองในหน้าใดหน้าหนึ่ง**

| กติกา | ค่า |
|---|---|
| หัวตาราง | พื้น `--navy-800` ตัวอักษรขาว · เส้นแบ่งคอลัมน์สีขาวจาง |
| เส้นแบ่งคอลัมน์ | `border-right: 1px solid var(--line-soft)` (คอลัมน์สุดท้ายไม่มี) |
| การจัดวาง | **กึ่งกลางทั้งหัวและข้อมูล** |

🔴 คอลัมน์ที่ต้องชิดซ้าย/ขวาเป็นพิเศษ ใส่คลาสที่ `<th>`/`<td>`

- `.txt-left` — ชื่อรายการยาวๆ อ่านง่ายกว่าเมื่อชิดซ้าย
- `.txt-right` — จำนวนเงิน อ่านง่ายกว่าเมื่อชิดขวา

**หัวคอลัมน์สุดท้าย (ปุ่ม) ใส่ชื่อว่า "จัดการ" เสมอ** และใส่ `data-no-filter`


### จำนวนเงิน — ตัวหนา สีเขียว

ยอดเงินหลักใช้ `.money-strong` (ในตาราง) และ `.paper-amount` (ในกระดาษ) — ทั้งคู่ใช้ `var(--ok)`


### ข้อความยาวในตาราง ต้องตัดด้วย `...`

ชื่อรายการ/ชื่องบยาวได้ถึง 191 ตัวอักษร ปล่อยไว้ตารางจะยืดจนอ่านคอลัมน์อื่นไม่ทัน

```css
.cell-title { max-width: 300px; overflow: hidden; text-overflow: ellipsis; white-space: nowrap; }
```

ใส่ `title="{{ $row->title }}"` ไว้ด้วยเสมอ เพื่อให้เอาเมาส์ชี้แล้วอ่านเต็มได้


### ชื่อแผนกต้องมีรหัสนำหน้า

เก็บและแสดงเป็น `IT · เทคโนโลยีสารสนเทศ` เสมอ — **ชื่อแผนกซ้ำกันได้ แต่รหัสไม่ซ้ำ**
เก็บลง `dept_name` ตอนสร้างเอกสารเลย จะได้ไม่ต้อง join ทีหลัง และชื่อไม่เพี้ยนถ้าแผนกเปลี่ยนชื่อ


### breadcrumb ไม่เติม "หน้าแรก" ให้อัตโนมัติ

แต่ละหน้าเขียนเส้นทางของตัวเองให้ครบใน `$trails` — เพราะ "หน้าแรก" คือหน้า `/guide`
ซึ่งเป็นคนละหน้ากับที่กำลังดูอยู่ (เจ้าของสั่งเอาออก 2026-09-02)

```
/guide            หน้าแรก
/dashboard        แดชบอร์ด › ภาพรวมระบบ
/profile          ข้อมูลส่วนตัว
/admin/external   ตั้งค่าระบบ › บัญชีผู้ใช้ภายนอก (Supplier)
```

> หน้า `/guide` (คำแนะนำการใช้งาน) **ไม่อยู่ในเมนูซ้าย** — เข้าจากปุ่ม "หน้าแรก" บนแถบเครื่องมือเท่านั้น

**การแยกหัวข้อหลัก/ย่อยด้วยสายตา** (เจ้าของสั่ง 2026-09-02) — ทำไว้ที่ธีมแล้ว ไม่ต้องทำเพิ่มต่อหน้า

| ชั้น | หน้าตา |
|---|---|
| หัวข้อหลัก | ตัวหนาสีน้ำเงินเข้ม · พื้นอ่อน (`--surface-2`) ตอนกางอยู่ · **มีเส้นคั่นด้านล่างแยกแต่ละกลุ่ม** |
| หัวข้อย่อย | เยื้องเข้า 26px · **วาดเป็นผังต้นไม้ — เส้นตั้ง + กิ่งแนวนอนของแต่ละแถว** (แถวสุดท้ายเส้นจบครึ่งบน ไม่ห้อยเลยลงไป) · ตัวปกติสีอ่อนกว่า |
| หน้าที่เปิดอยู่ | พื้น `--accent-soft` + ตัวหนาสีน้ำเงิน |

- **ธงภาษาอยู่ขวาสุดเสมอ** — ตำแหน่งเดียวกับหน้าล็อกอิน ผู้ใช้จะได้หาเจอที่เดิมทุกหน้า (เจ้าของสั่ง 2026-09-02)
- **ไม่มีปุ่มเฟืองตั้งค่าบนแถบบน** (เอาออกแล้ว) — เมนูตั้งค่าอยู่ในเมนูซ้ายกลุ่ม `3. ตั้งค่าสิทธิ์การเข้าใช้งาน`
- เมนูใต้ชื่อผู้ใช้มี 2 รายการ: **ข้อมูลส่วนตัว** (`route('profile')`) และ **ออกจากระบบ**

---

## 6A2. 🎨 ปุ่มยืนยัน + ไอคอนหัวข้อย่อย

### ปุ่มยืนยัน — สีบอกผลก่อนอ่านตัวหนังสือ

| ปุ่ม | คลาส | ใช้เมื่อ |
|---|---|---|
| **อนุมัติ / ผ่าน** | `.btn .btn-ok` | เขียว |
| **ปฏิเสธ / ลบ** | `.btn .btn-danger` | แดง |
| ทั่วไป | `.btn` | น้ำเงินองค์กร |

🔴 **`.btn-ok` อยู่ที่ธีมกลางแล้ว** — เคยไปนิยามไว้ในหน้า `/budget/approval` หน้าเดียว
หน้าอื่นใส่คลาสแล้วไม่เขียว (บั๊กจริง 2026-09-04) · **สไตล์ที่ใช้ข้ามหน้าต้องอยู่ธีมกลางเสมอ**

### ไอคอนของหัวข้อย่อย

หัวข้อย่อยใน `NavMenu` ใส่คีย์ `icon` ได้ ไม่ใส่ = ไอคอนเอกสารเปล่า (`file`)

| ไอคอน | ใช้กับ |
|---|---|
| `fn-history` | 🔴 **หัวข้อ "ประวัติ..." ของทุกโมดูล** (ประวัติงบประมาณ · ประวัติ PR · …) ใช้ตัวเดียวกันหมด |
| `fn-budget-approved` | Budget ที่อนุมัติแล้ว |
| `fn-settings` | 🔴 **หัวข้อย่อยของงานตั้งค่า** (สิทธิ์การเข้าถึงระบบ BMS · สิทธิ์การเข้าถึงโมดูล · หน้าตั้งค่าอื่นๆ ต่อไป) |

ไฟล์อยู่ที่ `public/img/nav/` · ทะเบียนชื่ออยู่ที่ [`layouts/partials/nav-icon.blade.php`](resources/views/layouts/partials/nav-icon.blade.php)
🔴 **ย่อเป็น 72px ก่อนวางเสมอ** — ต้นฉบับที่เจ้าของให้มากว้าง ~1250px เกือบ 1 MB เมนูใช้แค่ 18px
ตัดขอบขาวรอบนอกออกด้วย ให้เต็มกรอบเท่าไอคอนตัวอื่น

---

## 6B0. 🧭 เส้นทางเอกสาร ("สถานะทั้งหมด") — 4 ด่าน

```
1 สร้างเอกสาร  →  2 ผู้รับทราบ  →  3 สถานะ Invest  →  4 Budget อนุมัติ
  ผู้เสนอ+วันที่      รูป+ชื่อเท่านั้น      ผู้ลงนาม+วันที่ที่เซ็น      เลขที่งบ+วันที่
```

🔴 **ด่าน "ผู้รับทราบ" ขึ้นแค่รูปโปรไฟล์กับชื่อ-สกุล — ไม่มีวันที่ ไม่มีป้ายสถานะ**
ด่านนี้ไม่ได้ขวางเอกสาร ชื่อด่านบอกอยู่แล้วว่าเป็นผู้รับทราบ
ปล่อยให้สายตาไหลผ่านไปหาด่านผู้ลงนามเลย (เจ้าของสั่ง 2026-09-04)
วงกลมกับเส้นเชื่อมยังเป็นสีเขียวตามปกติ จะได้เห็นว่าเอกสารผ่านด่านนี้ไปแล้ว

🔴 **ผู้ลงนามที่ยังไม่เซ็น ไม่ต้องเขียน "รอลงนาม" ใต้ชื่อ** — ป้ายของด่านบอกอยู่แล้ว เขียนซ้ำทุกคนทำให้รก
เขียนใต้ชื่อเฉพาะตอน **ลงนามแล้ว · วันที่** (เขียว) หรือ **ไม่อนุมัติ** (แดง)

- ชื่อคนต้องอยู่ชิดป้ายสถานะ 🔴 **ห้ามใส่ `margin-top:auto` ที่ `.route-foot`** จะดันชื่อไปกองล่างสุด
- 🔴 **ด่านที่ไม่มีป้าย ต้องใส่ที่ว่างแทนป้าย** (`.route-state-ghost`) ไม่งั้นรายชื่อของด่านนั้นจะลอยสูงกว่าด่านอื่น ดูไม่เป็นแถวเดียวกัน
- ทุกบรรทัดสถานะต้องมีคีย์แปล 2 ภาษา (ส่งผ่าน `noteKey` ของ `route-person` · วันที่ส่งแยกทาง `noteTail`)
- 4 ด่านต้องพอดีกรอบหน้าต่าง (กว้าง 760px) ไม่ต้องเลื่อนดูด่านสุดท้าย
- **ป้ายสถานะของแต่ละด่านมีกรอบสีตามสถานะ** (เขียว = ผ่านแล้ว · เหลือง = กำลังรอ · แดง = ตีกลับ · เทา = ยังไม่ถึง)
  🔴 **ขนาดพอดีข้อความเสมอ** — `inline-block` + padding เล็ก ห้ามยืดเต็มความกว้างของด่าน
- 🔴 **ใต้ชื่อผู้ลงนามใส่ "วันเวลาที่ตัดสิน" ไม่ใช่คำว่าอนุมัติ/ไม่อนุมัติ** (เจ้าของสั่ง 2026-09-04)
  ป้ายของด่านบอกผลอยู่แล้ว · สีเขียว/แดง ของวันเวลาบอกได้ว่าเป็นการอนุมัติหรือปฏิเสธ
- **บรรทัดเหตุผล**: ป้าย `เหตุผล:` สีดำคงที่ · ตัวข้อมูลอยู่ในกรอบสีตามสถานะ ขนาดพอดีข้อความ
  🔴 ใช้คลาสกลาง `.doc-reason` / `.doc-reason-key` / `.doc-reason-val` ที่ [`budget/partials/assets.blade.php`](resources/views/budget/partials/assets.blade.php)
  **ทั้งในเส้นทางเอกสารและในตัวเอกสารต้องหน้าตาเหมือนกัน** — ห้ามใช้กล่อง `.flash` เต็มความกว้างอีก
  🔴 พื้นหลังเต็มช่องใช้เฉพาะ**คอลัมน์สถานะในตาราง**เท่านั้น (ข้อ 6B1)

---

## 6B0b. 🏷️ หมวดค่าใช้จ่าย — ใช้ชิ้นส่วนกลางทุกที่

ชิ้นส่วน [`budget/partials/category.blade.php`](resources/views/budget/partials/category.blade.php)
ใช้ทั้งในเอกสารและตาราง — เอกสารกับตารางต้องแสดงเหมือนกันเสมอ

```
อื่นๆ ( อุปกรณ์และทรัพย์สิน / Equipment )
```

🔴 **ข้อความที่ผู้ใช้กรอกเองต้องตัวอักษรขนาดเท่ากันและสีเดียวกับชื่อหมวด** ใส่ในวงเล็บ
ห้ามใช้คลาส `.soft` ทำให้จางลง — มันคือเนื้อหาจริงของเอกสาร ไม่ใช่คำอธิบายประกอบ (เจ้าของสั่ง 2026-09-04)

---

## 6B0c. 🔢 เลขที่เอกสาร + กลุ่มเอกสาร (เจ้าของสั่ง 2026-09-17)

| ชิ้นส่วน | ใช้ทำอะไร |
|---|---|
| [`budget/partials/doc-no`](resources/views/budget/partials/doc-no.blade.php) | เลขที่เอกสาร **หรือป้าย "ยังไม่ออกเลข"** (ตัวเอียงสีจาง) สำหรับร่าง · ใช้ทุกหน้าที่โชว์ร่าง |
| [`budget/partials/group-tabs`](resources/views/budget/partials/group-tabs.blade.php) | แท็บกรองกลุ่มเอกสาร (ทั้งหมด · ชื่อกลุ่ม + ป้ายโค้ด + จำนวน) · ข้อมูลมาจาก `DocGroupTabs::tabs()` |

- 🔴 **เลขที่ INV ออกตอนกดส่ง** — ร่างไม่มีเลข ห้ามปล่อยช่องว่าง ต้องใช้ `doc-no` เสมอ
- 🔴 **ตัวอย่างเลขที่ลงท้าย `000001` เสมอ** ไม่ใช่เลขถัดไปจริง (2 คนเปิดพร้อมกันจะเห็นเลขเดียวกัน) · หน้ากรอกเขียนเลขวิ่งเป็น `XXXXXX`
- **ป้ายโค้ดกลุ่ม** = กรอบ `--accent` พื้น `--accent-soft` ตัว `--navy-800` ตัวหนา เว้นระยะอักษร · ชุดสีเดียวทุกหน้า จับคู่ด้วยสายตาได้
- แท็บกลุ่มที่ **ปิดใช้งานแต่ยังมีเอกสาร** = ชื่อขีดฆ่า · ไม่มีเอกสารแล้วไม่ต้องมีแท็บ
- ฟอร์มตัวกรองในหน้าที่มีแท็บ **ต้องพา `group` ติดไปด้วย** (`<input type="hidden" name="group">`)
- ป้ายนับจำนวนใช้ `--radius-sm` **ไม่ใช่วงรี 999px** (เจ้าของไม่ชอบมุมโค้งมาก)

🔴 **คอมเมนต์ Blade ห้ามซ้อนกัน** — คอมเมนต์จบที่ตัวปิดตัวแรกที่เจอ ข้อความที่เหลือจะหลุดไปวาดบนหน้าโดยไม่มี error
(บั๊กจริง 2 ครั้ง · มีเทสต์ `BladeCommentTest` กวาดทุกไฟล์แล้ว) — อยากยกตัวอย่างโค้ดในคอมเมนต์ ให้เขียนเป็นข้อความธรรมดา

---

## 6B1. 📋 กติกาตารางทุกตารางในระบบ (เจ้าของสั่ง 2026-09-04)

### ตัวแบ่งหน้า

🔴 **ห้ามใช้ตัวแบ่งหน้ามาตรฐานของ Laravel** — มันเขียนด้วยคลาส Tailwind ซึ่งโปรเจคนี้ไม่ได้ใช้
ปุ่มจะกลายเป็นข้อความเปล่าไม่มีกรอบ และเป็นภาษาอังกฤษล้วน (ผิดกฎ 2 ภาษา) — **บั๊กจริง 2026-09-04**

ตั้งเป็นของระบบเองแล้วที่ `AppServiceProvider::boot()` → [`vendor/pagination/bms.blade.php`](resources/views/vendor/pagination/bms.blade.php)
หน้าใหม่แค่เรียก `{{ $rows->links() }}` ได้เลย 🔴 **ห้ามครอบด้วย `<div class="pager">` อีกชั้น** — view มีคลาสนั้นในตัวแล้ว

### 🔴 ห้ามใส่ตัวกรอง 2 ชุดในตารางเดียว

| แบบ | กรองอะไร | ใช้เมื่อ |
|---|---|---|
| `data-filterable` (▼ แบบ Excel) | เฉพาะแถวในหน้าปัจจุบัน (ฝั่งเบราว์เซอร์) | ตารางที่ไม่แบ่งหน้า หรือข้อมูลน้อย |
| แถวตัวกรองใต้หัวตาราง (ส่ง GET) | ทุกแถวในฐานข้อมูล (ฝั่งเซิร์ฟเวอร์) | ตารางที่แบ่งหน้า เช่นประวัติการใช้งาน |

ใส่ทั้ง 2 แบบพร้อมกันแล้วผลไม่ตรงกัน ผู้ใช้กรองแล้วได้คนละคำตอบ (บั๊กจริงที่หน้าประวัติการใช้งาน)

### ตารางในการ์ดหน้าแรก

🔴 **การ์ดกว้างครึ่งเดียวตามผัง** (เจ้าของยืนยัน 2026-09-04) — หน้านี้เป็นหน้ารวมผลสรุป จะมีการ์ดอื่นมาวางข้างๆ
พื้นที่ครึ่งเดียว (~570px) ใส่ได้ราว **7 คอลัมน์** — เกินกว่านั้นให้**ตัดคอลัมน์ออกจากการ์ด** ไปดูที่หน้าเต็มแทน
🔴 **ห้ามแก้ด้วยการปล่อยให้เลื่อนแนวนอน**

🔴 **หัวคอลัมน์ในการ์ดต้องขึ้นบรรทัดใหม่ได้** (`white-space: normal`)
ชื่อหัวคอลัมน์ยาว (เช่น "ชื่อรายการที่ขอตั้งงบ") ถ้าบังคับบรรทัดเดียว มันจะดันความกว้างตารางจนล้น
**ไม่ว่าจะบีบเซลล์แค่ไหนก็ไม่พอ** — บทเรียนจริง 2026-09-04

🔴 **มียอดรวมในการ์ด = ยอดต้องเดินตามตัวกรอง** ไม่งั้นตัวเลขไม่ตรงกับแถวที่เห็น
ตัวกรองยิง event `table:filtered` ออกมา (`detail.rows` / `shown` / `total`) หน้าที่มียอดรวมฟังแล้วคิดใหม่
🔴 บวกจาก `data-amount` ที่แถว **ห้ามแกะตัวเลขจากข้อความในเซลล์** (มีลูกน้ำ และรูปแบบอาจเปลี่ยนทีหลัง)
ล้างตัวกรองแล้วคืนยอดรวมของทั้งหมดที่เซิร์ฟเวอร์คิดมา (การ์ดแสดงแค่ 50 รายการล่าสุด ยอดของแถวที่เห็นจะขาด)


🔴 **ต้องเห็นครบทุกคอลัมน์โดยไม่ต้องเลื่อนแนวนอน** (เจ้าของสั่ง 2026-09-04)
การ์ดแคบกว่าหน้าเต็ม จึงย่อตัวอักษรเป็น `--fs-xs` · ลด padding เหลือ `7px 6px` · **ไม่ตั้ง `min-width` ให้ตาราง**
ข้อความยาว (ชื่องบ · แผนก) ตัดด้วย `...` พร้อม `title=` ไว้ให้ชี้ดูเต็มๆ
กติกานี้ใช้เฉพาะตารางในการ์ด — ตารางในหน้าเต็มยังใช้ขนาดปกติ


🔴 **ทุกตารางต้องมีคอลัมน์ "ลำดับ" เป็นคอลัมน์แรก** — เลข 1,2,3,… จัดกึ่งกลาง

```blade
<th class="col-seq" data-no-filter data-i18n="tbl.seq">ลำดับ</th>
...
<td class="col-seq">{{ $rows->firstItem() + $loop->index }}</td>   {{-- ตารางที่แบ่งหน้า --}}
<td class="col-seq">{{ $loop->iteration }}</td>                     {{-- ตารางที่ไม่แบ่งหน้า --}}
```

- 🔴 **ตารางที่แบ่งหน้าต้องนับต่อเนื่องข้ามหน้า** ใช้ `firstItem() + $loop->index`
  ใช้ `$loop->iteration` เฉยๆ หน้า 2 จะเริ่มนับ 1 ใหม่
- ใส่ `data-no-filter` ที่หัวคอลัมน์ — ไม่มีใครกรองด้วยเลขลำดับ
- ตารางที่มีแถวย่อยกาง/พับ (เช่น `/access/modules`) **แถวย่อยเว้นช่องลำดับไว้ว่าง** ไม่ให้เลขซ้อนกัน
- สไตล์ `.col-seq` อยู่ที่ [`theme.blade.php`](resources/views/layouts/theme.blade.php) — ห้ามเขียนซ้ำในหน้าใดหน้าหนึ่ง

🔴 **คอลัมน์สถานะต้องระบายพื้นหลังเต็มช่อง**

```css
/* ระบายที่ "ตัวช่อง" ไม่ใช่ที่ป้าย — ป้ายคลุมได้แค่ความสูงของตัวเอง */
@supports selector(td:has(> span)) {
  .tbl td:has(> .st) { padding: 0; }
  .tbl td:has(> .st) > .st { margin: 0; background: transparent; }
  .tbl td:has(> .st-pending) { background: #fdf2e0; }
}
```

⚠️ **บทเรียน:** เคยยืดตัวป้ายด้วย margin ติดลบเท่ากับ padding ของช่อง
แต่ความสูงของแถวมาจากคอลัมน์อื่นที่ตัวหนังสือขึ้น 2-3 บรรทัด ป้ายจึงเหลือขาวบน-ล่าง
**ต้องระบายที่ `td` เท่านั้น** · เก็บกฎเดิมไว้เป็นทางถอยสำหรับเบราว์เซอร์ที่ยังไม่รองรับ `:has()`

---

## 6B2. 💰 จำนวนเงิน — ต้องมีลูกน้ำเสมอ (เจ้าของสั่ง 2026-09-04)

🔴 **ทุกจำนวนเงินที่ผู้ใช้เห็น ต้องคั่นหลักพันด้วยลูกน้ำ ไม่มีข้อยกเว้น**
ครอบคลุมทั้ง **ที่แสดงผล** และ **ที่กำลังพิมพ์อยู่ในช่องกรอก**

### ที่แสดงผล (ตาราง · เอกสาร · การ์ด · แถบสรุป)

```blade
<span class="money money-strong">{{ number_format($row->amount, 2) }}</span>
```

- ใช้ `number_format($x, 2)` เสมอ — **ทศนิยม 2 ตำแหน่งตายตัว** (`180,000.00` ไม่ใช่ `180,000`)
- ตัวเลขในตารางชิดขวาด้วย `.txt-right` · ตัวหนาสีเขียวด้วย `.money-strong`
- 🔴 **ห้ามพ่นค่าดิบ** `{{ $row->amount }}` ลงหน้าจอ

### ที่กรอก (ใส่ลูกน้ำให้ระหว่างพิมพ์)

ชิ้นส่วนกลาง [`layouts/partials/money-input.blade.php`](resources/views/layouts/partials/money-input.blade.php)
include ไว้ที่ `layouts/app.blade.php` แล้ว **ทุกหน้าได้ไปด้วย ไม่ต้อง include ซ้ำ**

```blade
<span class="money-field">
  <input class="input money-box" type="text" inputmode="decimal" required
         data-money-input data-money-target="amount"
         value="{{ $v === '' ? '' : number_format((float) $v, 2) }}">
  <span class="money-unit" data-i18n="budget.baht">บาท</span>
</span>
<input type="hidden" name="amount" id="amount" value="{{ $v }}">
```

| กติกา | เหตุผล |
|---|---|
| 🔴 ช่องที่ผู้ใช้เห็นเป็น `type="text"` เสมอ | `type="number"` ใส่ลูกน้ำไม่ได้ เบราว์เซอร์จะถือว่าค่าผิดแล้วส่งค่าว่างมาแทน |
| 🔴 ค่าที่ส่งจริงอยู่ใน `<input type="hidden">` | เป็นตัวเลขล้วนเสมอ ฝั่งเซิร์ฟเวอร์ validate `numeric` ได้ตามปกติ |
| 🔴 ฝั่งเซิร์ฟเวอร์ **ยังต้องตัดลูกน้ำทิ้งก่อน validate** | เผื่อ JS ไม่ทำงาน หรือมีคนยิงค่าตรงเข้ามา |
| ต้องคืนตำแหน่งเคอร์เซอร์เองหลังจัดรูปแบบ | เขียนทับ `value` แล้วเคอร์เซอร์เด้งไปท้ายช่อง พิมพ์แทรกกลางตัวเลขไม่ได้ |
| ออกจากช่องแล้วเติมทศนิยมให้ครบ 2 ตำแหน่ง | ให้ตรงกับที่แสดงในตาราง |

**หน้าตาช่องเงินตั้งใจให้ต่างจากช่องอื่น** — พื้นเขียวอ่อน (`--ok-soft`) · ตัวหนาสีเขียว (`--ok`) ·
ชิดขวา · ตัวเลขความกว้างเท่ากัน (`tabular-nums`) · มีคำว่า "บาท" ลอยอยู่ในช่อง
เพราะเป็นตัวเลขที่สำคัญที่สุดในเอกสาร ต้องกวาดตาเจอทันที

---

## 6B3. 🧾 คนในตัวเอกสาร (กระดาษ)

🔴 **ทุกที่ที่เอ่ยชื่อคนในกระดาษ ต้องมีรูปโปรไฟล์อยู่บรรทัดเดียวกับชื่อ และกดขยายได้**

| จุด | ใช้ชิ้นส่วน |
|---|---|
| แถว "ผู้เสนอ" ในกระดาษ | `budget/partials/paper-person.blade.php` |
| รายชื่อ "สำเนาเรียน (เพื่อทราบ)" | `budget/partials/person-chip.blade.php` ครอบด้วย `.cc-item` |
| คนในเส้นทางเอกสาร (timeline) | `budget/partials/route-person.blade.php` |

🔴 **อย่าใช้ `route-person` ในหน้ากระดาษ** — คลาส `.route-*` มีสไตล์อยู่ในชิ้นส่วนเส้นทางเอกสารเท่านั้น
หน้ากระดาษไม่ได้โหลดไปด้วย รูปกับชื่อจะหล่นคนละบรรทัด (บั๊กจริง 2026-09-04)

🔴 **สำเนาเรียนต้องขึ้นในตัวเอกสารด้วย** ทั้งใบขออนุมัติตั้งงบและใบงบที่อนุมัติแล้ว
ผู้อ่านต้องรู้ว่าเรื่องนี้ส่งถึงใครไว้เพื่อทราบบ้าง · กลุ่มนี้ไม่มีช่องเซ็น

### บทเรียน: สไตล์ที่ใช้ข้ามโมดูลต้องอยู่ที่ธีมกลาง

`.avatar` เคยอยู่ในชิ้นส่วน `access/partials/picker-assets` ของโมดูลสิทธิ์
หน้าไหนไม่ได้ include ตัวนั้น **รูปพนักงานขึ้นเต็มจอ** (เจอจริงที่หน้าเอกสารงบ 2026-09-04)
→ ย้ายไป [`theme.blade.php`](resources/views/layouts/theme.blade.php) แล้ว **อย่าย้ายกลับ**

---

## 6C. ข้อมูลที่มาจาก Insight = อ่านอย่างเดียว

หน้า `/profile` และทุกหน้าที่แสดงข้อมูลพนักงาน/บัญชี **ห้ามทำฟอร์มให้แก้ในBMS**

ข้อมูลเป็นมิเรอร์ทางเดียวจาก Insight — แก้ที่ BMS ไปก็ถูกทับกลับตอนซิงค์รอบถัดไปอยู่ดี
ให้ขึ้นป้าย `อ่านอย่างเดียว / Read only` แล้วมีปุ่มลิงก์ไปแก้ที่ Insight (`config('bms.insight_url')`) แทน

---

## 6D. เอฟเฟกต์เคลื่อนไหว — กติกา

หน้า `/profile` ทำเป็น **นามบัตร 3 มิติเอียงตามเมาส์** (เจ้าของสั่ง 2026-09-02)
ใช้เป็นตัวอย่างได้เวลาต้องทำเอฟเฟกต์แบบเดียวกันที่อื่น

วิธีทำ: JS อัปเดต **ตัวแปร CSS** (`--rx --ry --mx --my --lift`) แล้ว CSS เอาไปใช้เอง
JS ไม่ยุ่งกับ `style.transform` ตรงๆ — แยกหน้าที่ชัด แก้หน้าตาได้โดยไม่ต้องแตะ JS

| สิ่งที่ต้องทำ | เหตุผล |
|---|---|
| รวบการอัปเดตไว้เฟรมเดียวด้วย `requestAnimationFrame` | ไม่เขียน style ทุกครั้งที่เมาส์ขยับ (เมาส์ยิง event ถี่กว่าจอวาดมาก) |
| ตอนเมาส์อยู่บนวัตถุใช้ transition สั้น (~.08s) · ตอนเมาส์ออกใช้ยาว (~.5s) | ตามเมาส์ต้องทันที แต่ตอนคืนตำแหน่งต้องนุ่ม |
| เงาแยกเป็น element ของตัวเอง | จะได้ขยับสวนทางกับการเอียง เหมือนเงาจริง |
| ชั้นที่อยากให้ลอย ใช้ `translateZ()` ต่างกัน | ได้ parallax จริง ไม่ใช่ภาพแบนบิดไปมา |
| ข้ามเอฟเฟกต์เมื่อ `pointerType === 'touch'` | จอสัมผัสไม่มีเมาส์ กดค้างแล้วบัตรเอียงค้างจะดูแปลก |

🔴 **ต้องเคารพ `prefers-reduced-motion: reduce` เสมอ** — ผู้ใช้ที่ตั้งค่านี้มักเวียนหัวกับภาพเคลื่อนไหว
ในหน้าโปรไฟล์ทำไว้ 2 ชั้น: CSS ตัด transform ทิ้ง และ JS `return` ตั้งแต่ต้นไม่ผูก event เลย


### เมนูซ้าย — กาง/พับต้องมีอนิเมชันเลื่อน (เจ้าของสั่ง 2026-09-03)

🔴 **อย่าคุมความสูงเป็น px ด้วย JS** — เคยทำแล้วพัง เพราะต้องอ่าน `scrollHeight` ตอน `<details>` ยังปิดอยู่
ได้ค่า 0 บ้าง ไม่ตรงบ้าง และกดรัวๆ แล้วค้างครึ่งทาง (`transitionend` ยิงจาก property อื่นด้วย)

**วิธีที่ใช้จริง** — ให้ CSS คำนวณความสูงเอง

```css
.nav-group > .nav-items { display: grid; grid-template-rows: 1fr; transition: grid-template-rows .24s var(--ease); }
.nav-group > .nav-items > * { min-height: 0; }
.nav-group.is-closing > .nav-items,
.nav-group.is-opening > .nav-items { grid-template-rows: 0fr; opacity: 0; }
```

JS เหลือหน้าที่แค่ 2 อย่าง: **หน่วงการปิด `<details>` ไว้ 240ms** ให้อนิเมชันเล่นจบ
และ **กันการกดซ้ำระหว่างเล่น** (เช็คว่ามีคลาส `is-closing` / `is-opening` อยู่ไหม)

**ตารางใช้วิธีนี้ไม่ได้** — `<tr>` เป็น `display: table-row` ทำ transition ความสูงไม่ได้ ให้ใช้จางเข้าไล่ทีละแถวแทน

---

## 7. การเข้าถึง (accessibility)

- ทุกปุ่มไอคอนต้องมี `aria-label` + `data-i18n-aria`
- `:focus-visible` มีกรอบชัดอยู่แล้วในธีม — **ห้ามสั่ง `outline: none` ทิ้ง**
- ไอคอนตกแต่งใส่ `aria-hidden="true"`
- ธีมเคารพ `prefers-reduced-motion` แล้ว
- สีอย่างเดียวห้ามเป็นตัวบอกสถานะเพียงอย่างเดียว — ต้องมีข้อความกำกับด้วย (`.pill` มีข้อความอยู่แล้ว)

---

## 8. ระวัง (บทเรียนจริงจาก Insight)

- **CSS specificity** — `.a-class` (0,1,0) แพ้ `table td.a-class` (0,1,2) สั่งจัดแนวแล้วไม่ติดให้เช็ค selector ที่ทับก่อน **อย่าเติม `!important` มั่ว**
- **ห้ามใช้ regex กว้างๆ แทนบล็อกโค้ดใหญ่** — เคยลบ CSS หาย 340 บรรทัดแล้วกู้ไม่ได้ ให้แทนที่ด้วยสตริงเต็มบล็อกและเช็คว่าเจอพอดี 1 จุดก่อนเขียนไฟล์
- **ไฟล์ view ยาวมากแล้วจะแยกไฟล์ ต้องถามเจ้าของก่อน**

---

## 9. Changelog

| วันที่ | สิ่งที่เปลี่ยน |
|---|---|
| 2026-09-04 | **ตัวแบ่งหน้าของระบบ (ข้อ 6B1)** — เลิกใช้ของ Laravel ที่เป็น Tailwind · ทำเองให้เข้าธีมและมี 2 ภาษา ตั้งเป็นค่าเริ่มต้นทุกหน้า · เพิ่มกฎ "ห้ามใส่ตัวกรอง 2 ชุดในตารางเดียว" |
| 2026-09-04 | **หน้า `/access/modules` แยกแถวสิทธิ์ย่อย** — สิทธิ์ย่อยมีแถวของตัวเอง ย่อหน้าลึกกว่าหัวข้อย่อยอีกชั้น พร้อมข้อมูลของตัวเองในบรรทัดเดียวกัน 🔴 ห้ามเอาไปต่อท้ายชื่อด้วย `/` อีก ผู้ดูแลระบบจะแยกไม่ออกว่าตั้งสิทธิ์ของอันไหน · เพิ่มไอคอน `fn-settings` (เฟือง) สำหรับหัวข้อย่อยของงานตั้งค่า |
| 2026-09-04 | **ปุ่มยืนยัน + ไอคอนหัวข้อย่อย (ข้อ 6A2)** — `.btn-ok` (เขียว) ย้ายขึ้นธีมกลาง ใช้คู่กับ `.btn-danger` (แดง) · หัวข้อย่อยกำหนดไอคอนของตัวเองได้ผ่านคีย์ `icon` ใน NavMenu · `fn-history` ใช้กับหัวข้อ "ประวัติ..." ของทุกโมดูล |
| 2026-09-04 | **เส้นทางเอกสารไม่มีกรอบสีแล้ว** — ป้ายสถานะและเหตุผลที่ไม่อนุมัติเหลือแค่ตัวอักษรสี (เขียว/เหลือง/แดง/เทา) · วงกลมเลขลำดับกับเส้นเชื่อมยังเป็นสีทึบ · พื้นหลังเต็มช่องใช้เฉพาะคอลัมน์สถานะในตาราง (ข้อ 6B0) |
| 2026-09-04 | **กติกาตาราง (ข้อ 6B1)** — ① ทุกตารางต้องมีคอลัมน์ **"ลำดับ"** เป็นคอลัมน์แรก จัดกึ่งกลาง · ตารางแบ่งหน้าต้องนับต่อเนื่องข้ามหน้า ② **คอลัมน์สถานะระบายพื้นหลังเต็มช่อง** ต้องระบายที่ `td` ด้วย `:has()` ไม่ใช่ยืดตัวป้ายด้วย margin ติดลบ (ป้ายคลุมได้แค่ความสูงตัวเอง) ③ เส้นทางเอกสาร: ชื่อคนไม่ดันลงล่างสุดแล้ว · **ผู้รับทราบมีสถานะของตัวเองขึ้นสีเขียว** เรียงลงมาทีละคน |
| 2026-09-04 | **กระดิ่ง 2 หน้าจอ + คนในกระดาษ** — ① แผงกระดิ่งเปลี่ยนจากกางซ้อนเป็น **สลับหน้า**: หน้ารายการโมดูลแบบรายการแอป → กดแล้วไปหน้าของโมดูลที่มี **แท็บหัวข้อย่อย** และปุ่ม `‹` ย้อนกลับ (ข้อ 6B) ② **คนในกระดาษต้องมีรูปบรรทัดเดียวกับชื่อ** ใช้ `paper-person` ห้ามใช้ `route-person` ③ **สำเนาเรียนต้องขึ้นในตัวเอกสารทั้ง 2 ใบ** ④ ย้าย `.avatar` ไปธีมกลาง เพราะหน้าที่ไม่ได้ include picker-assets รูปขึ้นเต็มจอ (ข้อ 6B3) |
| 2026-09-04 | **จำนวนเงินต้องมีลูกน้ำทุกที่** (ข้อ 6B2) — ที่แสดงผลใช้ `number_format($x, 2)` เสมอ · ที่กรอกใส่ลูกน้ำให้ระหว่างพิมพ์ด้วยชิ้นส่วนกลาง `money-input` (ช่องที่เห็นเป็น text · ค่าจริงอยู่ในช่องซ่อน · เซิร์ฟเวอร์ยังต้องตัดลูกน้ำเองอีกชั้น) · ช่องเงินหน้าตาต่างจากช่องอื่น พื้นเขียวอ่อน ตัวหนาสีเขียว ชิดขวา มีหน่วย "บาท" ในช่อง |
| 2026-09-04 | **กระดิ่งแจ้งเตือน + เก็บงานรอบ 3:** ① กระดิ่งบนแถบบนมีเลขแดงตัวอักษรขาวมุมขวาบนแบบเฟสบุค · แผง 3 ชั้น (โมดูล → หัวข้อย่อย → รายการ) อ่านหัวข้อย่อยจาก `NavMenu::menuFunctions()` ตัวเดียวกับเมนูซ้าย ② **คอลัมน์สถานะระบายพื้นหลังเต็มช่อง** ด้วย margin ติดลบเท่า padding ของ `td` — รออนุมัติเหลือง · อนุมัติเขียว · ตีกลับแดง · ร่างเทา ③ ตารางรับทราบ/อนุมัติเพิ่มคอลัมน์ "ต้องทำอะไร" (ลงนาม / รับทราบ / ดูอย่างเดียว) เพราะตารางเดียวถือหลายบทบาท ④ เส้นทางเอกสารต้องมีรูปผู้เสนอที่ด่าน "สร้างเอกสาร" ด้วย |
| 2026-09-03 | **รอบเก็บงาน 2:** ① ตัวกรองรายคอลัมน์แบบ Excel ทุกตาราง (`data-filterable`) ② หัวตารางพื้นน้ำเงินตัวอักษรขาว ③ จำนวนเงินตัวหนาสีเขียว ④ เส้นทางเอกสารเปลี่ยนเป็นแนวนอนถอดแบบจาก QuoteCompare ⑤ ฟอร์มตรวจก่อนบันทึก เลื่อนไปช่องที่ผิดและตีกรอบแดง |
| 2026-09-03 | **รอบเก็บรายละเอียด:** ① คอลัมน์ "สถานะทั้งหมด" — เส้นทางเอกสาร 3 ด่านแบบ QuoteCompare (🟢🟡🔴) ② ข้อความยาวในตารางตัดด้วย `...` + `title=` ③ ชื่อแผนกมีรหัสนำหน้าเสมอ ④ หน้าเสนอ Invest มีช่องลงชื่อให้ผู้เสนอกดประทับเอง ⑤ หน้า Budget ที่อนุมัติแล้วเป็นกระดาษพร้อมลายเซ็นครบทุกคน |
| 2026-09-03 | **ยุบสิทธิ์เหลือ 2 ชั้น + เอกสารแบบกระดาษ:** ① ลบสิทธิ์ระดับโมดูล — แถวหัวข้อหลักเป็นแถวสรุป ไม่มีปุ่มแก้ไข · เห็นโมดูลคำนวณจากหัวข้อย่อย ② เพิ่ม middleware `bms.fn` กันสิทธิ์จริงฝั่งเซิร์ฟเวอร์ (เดิมกันแค่ทำเมนูเทา) ③ หน้าเดียวเปิดหลายบทบาทได้ด้วยคีย์ `menu` — แยกสิทธิ์แต่รวมเมนู ④ **หน้าเอกสารจัดแบบกระดาษ** โลโก้บริษัทหัวบน + ช่องลงชื่อท้ายกระดาษ + ลายเซ็นประทับตอนอนุมัติ |
| 2026-09-03 | **เจ้าของสั่ง 4 อย่าง:** ① ทุก Action ต้องเด้ง**หน้าต่างแจ้งกลางจอพร้อมอนิเมชัน** (✓ / ! / ✕) ห้ามใช้ `alert()`/`confirm()`/`prompt()` ② **ยินยอมซ้าย · ยกเลิกขวา** ทุกหน้าต่าง ③ หัวข้อย่อยใน `/access/modules` เปลี่ยนจาก Modal เป็น**แถวในตารางที่กางด้วยปุ่ม "V"** ④ ไฟล์แนบใช้**ชุดไอคอนเอกสาร** `public/img/file/*.png` ตามนามสกุล และต้องแสดงรายการไฟล์พร้อมปุ่ม ✕ เอาออก · เมนูซ้ายกาง/พับใช้ `grid-template-rows` ไม่คุมความสูงเป็น px |
| 2026-09-02 | **แถบบน + รูปโปรไฟล์:** ย้ายธงภาษาไปขวาสุด (ตำแหน่งเดียวกับหน้าล็อกอิน) · เอาปุ่มเฟืองตั้งค่าออก · แยกรูปโปรไฟล์เป็นปุ่มของตัวเอง (กดรูป = ขยาย · กดชื่อ = เปิดเมนู) · เพิ่มตัวขยายรูปกลาง `image-zoom` (หมุนล้อซูม · ลากเลื่อน · ดับเบิลคลิก · คีย์ลัด) ใช้ได้ทุกหน้า · เพิ่มหน้า `/profile` แบบอ่านอย่างเดียว · **ตั้งเป็นกฎ: รูปโปรไฟล์ทุกที่ต้องกดขยายและซูมด้วยล้อเมาส์ได้** (ข้อ 6A) |
| 2026-09-02 | **เจ้าของสั่งปรับ 4 อย่าง:** ① ใช้โลโก้ไฟล์จริง `public/img/bms-logo.png` วางบนพื้นน้ำเงินตรงๆ ไม่ใส่กรอบขาว และขยายให้ใหญ่ขึ้น ② ตัวสลับภาษาใช้ **ธงชาติ** แทนไอคอนลูกโลก ③ **ลดความโค้งทั้งระบบ** (`--radius` 10→4px · `--radius-sm` 6→3px · `--radius-lg` 16→8px) ④ หน้าล็อกอิน **เต็มจอ** ไม่มีพื้นเทารอบการ์ด และ **เอาลายจุดออกจากแผงน้ำเงิน เหลือแค่วงกลม** |
| 2026-09-02 | สร้างไฟล์นี้ · เจ้าของอนุมัติสไตล์ ERP desktop จากภาพต้นแบบ 2 ภาพ · วาง token ครบใน `theme.blade.php` · ทำหน้าล็อกอิน · โครงหน้า Dashboard (แถบบน · แถบเครื่องมือ · เมนูซ้าย · แถบสถานะ) · ระบบ 2 ภาษา TH/EN |

---

## กราฟ (Charts)

🔴 **ห้ามดึงไลบรารีกราฟจาก CDN** — เซิร์ฟจริงอยู่ในวงแลน เน็ตไม่ถึงแล้วกราฟหายทั้งหน้า
วาดเป็น SVG เอง ต้นแบบอยู่ที่ [`core/partials/budget-chart.blade.php`](resources/views/core/partials/budget-chart.blade.php)

| กติกา | รายละเอียด |
|---|---|
| **กรอบสูงคงที่** | `.chart-box { height: 186px }` — เปลี่ยนชนิดกราฟแล้วการ์ด**ต้องไม่ยืด/ไม่หด** ไม่งั้นการ์ดข้างๆ ขยับตาม |
| **กระดาษวาดขนาดเดียว** | การ์ด `570×186` · หน้าต่างขยาย `930×360` · ทุกชนิดวาดลงกรอบเดียวกันแล้วให้ SVG ย่อเอง (`preserveAspectRatio`) |
| **จานสี** | 10 สีคุมโทนน้ำเงินองค์กร วนซ้ำเมื่อรายการเกิน — **ห้ามสุ่มสี** ค่าสีอยู่ในตัวแปร `COLORS` ของกราฟ |
| **ตัวอักษรในกราฟ** | ใช้คลาส `.c-label` `.c-value` `.c-grid` ที่อ่านสีจาก token — **ห้ามเขียนสีดิบใน SVG** (ยกเว้นตัวหนังสือบนพื้นสีเข้มที่ต้องเป็น `#fff`) |
| **รายการเยอะ** | 🔴 **วาดครบทุกรายการเสมอ** แล้วให้กราฟย่อตัวเอง — ช่องแคบ→ตะแคงชื่อ 90° · แคบมาก→ไม่เขียนชื่อ/ตัวเลข · **ห้ามยุบเป็นก้อน "อื่นๆ"** (ทดสอบ 57 แผนกแล้วก้อนนั้นกลายเป็น 83% จนกราฟอ่านไม่ได้) · ย่อได้เฉพาะ**คำอธิบายสี** |
| **แถวรวมท้ายตาราง** | ใช้ `<tfoot>` ได้เลย ธีมตีเส้นบนให้แล้ว · ช่องแรกต้องใส่คลาสเดียวกับหัวคอลัมน์ (`col-seq`) ไม่งั้นความกว้างไม่ตรงแถวข้อมูล |
| **กดกราฟ** | เปิดหน้าต่างใหญ่ผ่าน `data-modal-open` ของระบบ (อย่าสั่ง `hidden=false` เอง จะหลุดจากกติกา Esc / คลิกพื้นหลัง) |
| **% ในกราฟ** | วงกลม/แถบสัดส่วนต้องมี % กำกับ · ชิ้นเล็กเกินไม่ต้องใส่ (ทับกันจนอ่านไม่ออก) ให้ดูที่คำอธิบายสีแทน |
| **กราฟงบ ใช้ไป/กันไว้/คงเหลือ** (2026-09-14) | แท่งเดี่ยวซ้อนสีบนราง = วงเงินงบ · สีจาก token `--chart-used` (แดง) `--chart-reserve` (เหลือง — เจ้าของระบุ) `--chart-left` (เขียว) `--chart-track` · ตัวอักษรบนพื้นขาวใช้ `--danger`/`--warn`/`--ok` · บนป้ายพื้นน้ำเงินใช้ `--danger-on-dark`/`--warn-on-dark`/`--ok-on-dark` · ต้นแบบ [`core/dashboard.blade.php`](resources/views/core/dashboard.blade.php) |

### ตัวกรองของหน้าแดชบอร์ด

🔴 **ตัวกรองอยู่นอกการ์ด และคุมทุกการ์ดพร้อมกัน** — ห้ามการ์ดไหนมีตัวกรองแผนกของตัวเอง

```js
document.addEventListener('dash:filter', function (e) { e.detail.dept });  // '' = ทุกแผนก
window.BMS.dashDept()                                                     // อ่านค่าปัจจุบัน
```

---

## ตัวกรองคอลัมน์ของตาราง

| ตารางแบบไหน | ใส่อะไรที่ `<table>` |
|---|---|
| ไม่แบ่งหน้า | `data-filterable` (กรองด้วย JavaScript) |
| 🔴 **แบ่งหน้า** | `data-filter-server` + `data-filter-key` ที่ `<th>` + `@include('layouts.partials.table-filter-data')` |

🔴 **ห้ามใส่ตัวกรอง 2 ชุดในตารางเดียว** (เคยพลาดที่ `/activity` — ผลไม่ตรงกัน)
🔴 **หน้าที่มียอดรวม ต้องคิดยอดใหม่ตามตัวกรองเสมอ** และเปลี่ยนป้ายให้ตรงกับสิ่งที่นับ

## ตัวแบ่งหน้า

🔴 มีที่เดียวคือ [`vendor/pagination/bms.blade.php`](resources/views/vendor/pagination/bms.blade.php) + `.pg` ในธีมกลาง
**ห้ามเขียนสไตล์ `.pager` ซ้ำในหน้าใดหน้าหนึ่ง** — เคยทำให้เลขหน้าปัจจุบันเป็นตัวหนังสือน้ำเงินบนพื้นน้ำเงิน มองไม่เห็นเลย
