{{--
  เลขแดงบอกจำนวนแจ้งเตือนที่ยังไม่อ่าน (เจ้าของสั่ง 2026-09-07)

  🔴 ใช้ชิ้นส่วนเดียวกันทั้งเมนูซ้าย เมนูย่อย และที่อื่นๆ ที่จะเพิ่มทีหลัง
     รูปแบบเดียวกับเลขแดงบนกระดิ่ง — แดง ตัวขาว เกิน 99 เขียน 99+

  🔴 วาดไว้เสมอแม้จำนวนเป็น 0 (แค่ซ่อนไว้) และติด data-badge-key ไว้ด้วย
     เพราะกด "อ่านทั้งหมด" ที่กระดิ่งแล้ว JS ต้องหาเจอเพื่อลบเลขให้ทันที
     ไม่งั้นเลขแดงค้างจนกว่าจะโหลดหน้าใหม่ (บั๊กจริง 2026-09-07)

  ต้องส่งมา
    $n    จำนวน
    $key  คีย์ไว้ให้ JS หาเจอ — โมดูล = "budget" · หัวข้อย่อย = "budget|fn.budget.invest"
--}}
<span class="nav-badge" data-badge-key="{{ $key ?? '' }}" @if (($n ?? 0) < 1) hidden @endif>{{ ($n ?? 0) > 99 ? '99+' : ($n ?? 0) }}</span>
